# Plan 4 — Scope Ledger

Last updated: 2026-08-02 (pré-flight)
Last updated by: main (contrôleur SDD)

## Requirements (extracted from spec §3 Success criteria)

| ID | Requirement | Status | Notes |
|---|---|---|---|
| R1 | Collections `prompts` + `skills` + schémas Zod (page bundles) — `astro build` valide les 2 schémas **ET** ≥ 2 prompts + ≥ 2 skills réels | In progress | T-A1 (schémas + helpers + tests) puis T-A2 (contenu réel). Schémas repris **verbatim** de la spec de design §3.3 et §3.4 — ni champ en plus, ni champ en moins. `draft` **existe** ici, contrairement à `projects`. |
| R2 | `/prompts` grille + filtre format/outil/tag — ≥ 2 cartes **ET** filtrer par `format` → seules les entrées correspondantes restent | Done | T-B1 (carte + grille) puis T-B2 (filtre). Carte = **variante** de `ProjectCard.astro`. Vérification du filtre par **comptage**, pas à l'œil. |
| R3 | Copie 1 clic du prompt sur une fiche — contenu du champ `prompt` au presse-papier **ET** feedback visuel | Done | T-B3. **Réutilise** le bouton de code-block du Plan 1 (spec §6.2) ; ce plan lui ajoute le **fallback** exigé par la spec §6.1, aujourd'hui absent. Vérifié en lisant le presse-papier, pas seulement le libellé du bouton. |
| R4 | `/prompts/<slug>` distingue fiche vs guide | Done | T-B3. Règle pure `shouldRenderPromptBlock`, testée unitairement, y compris le cas « fiche sans `prompt` » (le schéma laisse le champ optionnel sur les deux formats). |
| R5 | `/skills` grille + filtre tag/type — ≥ 2 cartes **ET** filtrer par tag → sous-ensemble correct | Pending | T-C1. Aucun nouveau moteur de filtrage : réutilise celui de T-B2. |
| R6 | `/skills/<slug>` rend instructions + `installCmd` copiable + repo + prompts liés | Pending | T-C2. `installCmd` rendu dans un `<pre><code>` sous `<article>` → bouton du Plan 1. |
| R7 | Relation `skills ↔ prompts` résolue, aucun `undefined` | Done | T-A1 (garde-fou `assertEntriesResolved`, déplacé dans `src/lib/references.ts`), T-A2 (données + preuve d'échec par sonde), T-C2 (rendu + preuve d'échec réelle). **Fait établi au Plan 3 : une référence cassée laisse le build VERT tant qu'aucune page ne la résout.** |
| R8 | Prompts & skills éditables via le CMS | Pending | T-D1 (config + garde-fou de test) puis **T-D2, geste utilisateur** : un agent ne peut ni fournir le PAT ni actionner le sélecteur de dossier natif (établi au Plan 2, reconfirmé au Plan 3). |
| R9 | Direction dark editorial-dev + responsive 375px — pas de scroll horizontal à 375px | Done | Contrôle 375px **dark et light** dans T-B1, T-B2, T-B3, T-C1 et T-C2 (pas de tâche dédiée : chaque tâche livrant du gabarit le vérifie). |

## Status legend
- **Done** — verified, criteria passed (link to commit SHA)
- **In progress** — actively being worked
- **Pending** — not yet started
- **Deferred** — moved to a later plan (must have an approved deviation)
- **Cut** — removed from scope (must have an approved deviation)

## Task index → requirement(s) covered

| Task | Phase | Covers |
|---|---|---|
| A1 | A | R1 (collections, schémas Zod, helpers + tests), R7 (garde-fou de résolution) |
| A2 | A | R1 (≥ 2 prompts + ≥ 2 skills réels), R7 (données des deux sens) |
| B1 | B | R2 (carte + grille `/prompts`), R9 |
| B2 | B | R2 (filtre format/outil/tag — moteur à facettes), R9 |
| B3 | B | R3 (copie + fallback), R4 (fiche vs guide), R9 · **+ addition n°2** (sens prompt → skills) |
| C1 | C | R5 (grille `/skills` + filtre tag/type), R9 |
| C2 | C | R6 (instructions + `installCmd` + repo + prompts liés), R7 (rendu), R9 · **+ addition n°1** (nav « Prompts »/« Skills ») |
| D1 | D | R8 (collections CMS + garde-fou de test) · **+ addition n°3** (`delete: false`) |
| D2 | D | R8 (création réelle via `/admin` — **action utilisateur**) |
| Z1 | Z | Audit `/anti-drift-planning:verify 4` (couverture R1–R9) |

## Additions au-delà de la lettre de la spec (à ratifier au gate de pré-flight)

Aucune n'est exigée par un critère R. Elles sont listées ici pour être approuvées ou coupées **explicitement**, jamais glissées en silence.

1. **Liens de nav « Prompts » et « Skills » activés** (T-C2) — `src/components/Header.astro:12-18` les rend aujourd'hui non cliquables (« Bientôt disponible »). Sans cette activation, `/prompts` et `/skills` ne sont atteignables qu'en tapant l'URL. Même addition, même justification que l'addition n°1 ratifiée au Plan 3 pour « Projets ».
2. **Sens `prompt → skills liés`** (T-B3) — R6 et R7 n'exigent que le sens **skill → prompts**. Le champ `relatedSkills` existe pourtant côté `prompts` (spec de design §3.3) et R7 s'intitule `skills ↔ prompts` avec une double flèche. Sans cette addition, `relatedSkills` reste un champ de données jamais affiché.
3. **`delete: false` sur les 2 nouvelles collections CMS** (T-D1) — le Plan 3 a établi (déviation **D08, approuvée**) que le CMS ne nettoie jamais les rétro-références et qu'une suppression depuis `/admin` casse `astro build` en produisant **0 page**. Le risque est identique ici (relation symétrique), mais c'est un choix **observable** que la spec du Plan 4 ne mentionne pas.

## Décisions d'implémentation tranchées par le plan (spec muette — ratification au gate)

- **Périmètre exact de la réutilisation du filtre** (spec §6.3) : le **prédicat** est généralisé en un moteur unique (`src/lib/facetFilters.ts`) dont `projectFilters.ts` devient un adaptateur ; le **script DOM** du Plan 3 n'est **pas** migré (aucun critère du Plan 4 ne l'exige, et le réécrire toucherait une page livrée et vérifiée). Résiduel assumé : deux scripts DOM au-dessus d'un moteur unique.
- **Fallback presse-papier** : `copy-code.ts` du Plan 1 n'a ni fallback ni feedback d'échec ; la spec §6.1 exige les deux. Ajoutés en T-B3, chemin nominal inchangé.
- **Ordre des grilles** : titre A→Z (ni date ni `featured` dans ces schémas).
- **Entrées `draft`** : écartées des listes et des liens (pas de route publique — les lier produirait un 404).
- **Fiche sans `prompt`** : le schéma n'est **pas** durci ; le rendu n'affiche simplement pas de bloc vide.
- **Pas de TOC sur les guides**, **pas de couverture d'image** (absente des schémas §3.3/§3.4).

## Updates log

(Append a one-liner each time the ledger is updated, with the new status and the commit SHA.)

- 2026-08-02 — **Ledger seeded (pré-flight)** — 9 lignes `Pending`. Branche `plan-4-librairies-prompts-skills` créée depuis `main` (`eb9deee`, tag `milestone-plan-3`, Plan 3 vérifié PASS). Plan d'implémentation écrit (`docs/anti-drift/plans/2026-07-19-plan-4-librairies-prompts-skills.md`), log de déviations créé. Baseline mesurée sur la branche : `npm test` **42 tests / 4 fichiers**, `npx astro check` **0 error / 0 warning / 33 hints**, `npx astro build` **9 pages**. **En attente de validation utilisateur avant T-A1**, avec trois questions ouvertes : le contenu réel de T-A2 (2 prompts + 2 skills, qu'un agent ne peut pas inventer), les trois additions ci-dessus, et une **contradiction relevée entre le prompt de bootstrap et la spec** — le prompt demande de taguer `milestone-plan-3` (déjà posé, Plan 3), alors que la spec §4 Phase Z et l'en-tête (`Target: v0.4.0 + milestone-plan-4`) désignent `milestone-plan-4`.
- 2026-08-02 — **GATE DE PRÉ-FLIGHT — plan validé par l'utilisateur.** Réponses explicites transcrites : (1) « **Validé, démarre T-A1** » ; (2) **les trois additions sont approuvées** — n°1 nav « Prompts »/« Skills » activée en T-C2, n°2 sens `prompt → skills liés` rendu en T-B3, n°3 `delete: false` sur les 2 nouvelles collections CMS en T-D1 ; (3) **matière de T-A2 = anti-drift** — 1 fiche (le prompt de bootstrap réellement utilisé), 1 guide (découper un projet en plans anti-drift), 1 skill (le plugin `anti-drift-planning` de l'utilisateur, v0.4.0, MIT) ; (4) **tag et version : `milestone-plan-4` + `v0.4.0`**, conformément à la spec §4 et à son en-tête — le prompt de session demandait `milestone-plan-3`, tag déjà posé sur `eb9deee` au Plan 3, et ne sera **pas** déplacé. Les trois additions étant ratifiées **avant** exécution, ce ne sont pas des déviations. **Point resté ouvert, à trancher avant T-A2 : le 2ᵉ skill réel exigé par R1 n'est pas encore nommé.** Il ne bloque pas T-A1 (schémas, helpers, tests — aucun contenu).
- 2026-08-02 — T-A1 (collections `prompts` + `skills`, schémas Zod, helpers, garde-fou de références) → **In progress** — R1 et R7 In progress.
- 2026-08-02 — **T-A1 → Done** — commits `b316b45` (implémentation) et `f0d9b17` (fix round 1). `src/content.config.ts` gagne `PROMPT_FORMATS` et les collections `prompts` + `skills` (schémas **verbatim** de la spec de design §3.3/§3.4) ; `src/lib/references.ts` accueille `assertEntriesResolved` (déplacée depuis `projects.ts`) ; `src/lib/prompts.ts` et `src/lib/skills.ts` (tri, agrégations, `sortAndFilterPrompts`/`sortAndFilterSkills` pures sur le modèle de `sortAndFilter`) ; 14 tests ajoutés. **Vérifié indépendamment par le contrôleur** : `vitest` **56/56** (42 baseline + 14), `astro check` **0 error / 0 warning** (57 hints, contre 33 avant — hints uniquement, aucun bloquant), `astro build` **11 pages**, et `diff` du corps d'`assertEntriesResolved` avant/après **vide** (le déplacement est bien pur).
  - **Revue de tâche : spec ❌ / qualité Changes requested** → 0 Critical, **2 Important**, 5 Minor. Le relecteur a confronté les deux schémas Zod à la **spec de design elle-même**, pas au brief : **9/9 champs `prompts` et 10/10 `skills` conformes**, aucun champ en trop, mêmes types, défauts et optionnalités. **I1** : le filtrage `draft` était exigé par le plan, utilisé par T-C2, et couvert par **aucun** test — inliné dans la fonction `async`, donc hors de portée d'un test unitaire. **I2** : le JSDoc d'`assertEntriesResolved` avait été **réécrit** au lieu d'être déplacé, perdant la citation du code d'Astro (`createGetEntry` fait `console.warn(…); return;`) et le locus `posts.ts:5` — les deux faits qui rendent le garde-fou justifiable et son échec diagnosticable. **Déviations D03 et D04 loggées avant le correctif.**
  - **Fix round 1/5 → tous les constats ADDRESSED**, 0 régression introduite. Re-revue ciblée : `diff` ligne à ligne entre `git show a4eb77d:src/lib/projects.ts` et `src/lib/references.ts` — **seule différence, les 2 lignes de la dernière phrase**, exactement ce qui était demandé.
  - **5 Minor déférés** à la revue finale de branche, enregistrés dans le ledger SDD : libellé de test imprécis (`prompts.test.ts:41`), couverture asymétrique de `skills.test.ts`, 4 fonctions dupliquées entre les deux jumeaux, commentaire au présent sur 2 usages pas encore écrits, et garde-fou CMS↔Zod aveugle aux 2 nouvelles collections jusqu'à T-D1 (dette de fenêtre assignée par le plan).
  - **R1 et R7 restent In progress** : R1 exige « ≥ 2 prompts + ≥ 2 skills réels » (T-A2) et R7 exige des données et un rendu (T-A2, T-C2). **T-A2 est bloquée côté utilisateur** : le 2ᵉ skill réel n'est pas encore désigné.
- 2026-08-02 — **Plan amendé AVANT exécution de T-A2** (donc pas une déviation — précédent Plan 3). Le gate a tranché : **2 prompts + 1 seul skill** (`anti-drift-planning`). Recherche faite dans l'environnement de l'utilisateur : c'est le **seul** skill lui appartenant et publiable (`author.name: bendevcat`, `license: MIT`, `homepage: https://github.com/bendevcat/anti-drift-planning`) ; les autres vivent sous l'org **privée `sxd-platform`** avec `author.name: "Sixense Digital"` et `license: "UNLICENSED"` — établi en lisant les `plugin.json` réels. **Déviation D05 loggée avant exécution** (`pending-user`, réversibilité `cheap`). **Portée exacte, mesurée :** tombent la clause « ≥ 2 skills » de **R1** et « ≥ 2 cartes » de **R5** ; **R6 et R7 restent entièrement atteignables** avec 1 skill relié à 2 prompts — T-C1 et T-C2 sont donc construites et vérifiées normalement. Le skill portera `relatedPrompts` vers **les deux** prompts, pour que R7 ne repose pas sur un cas unique.
- 2026-08-02 — T-A2 (2 prompts réels + 1 skill réel + relation `skills ↔ prompts`) → **In progress**.
- 2026-08-02 — **Action utilisateur en attente, à rejouer avant la Phase Z : publier le dépôt `anti-drift-planning`.** État vérifié le 2026-08-02 sur trois canaux : `https://github.com/bendevcat/anti-drift-planning` répond **404** (direct, `-L`, et API GitHub), le dépôt local n'a **aucun remote**, et `gh repo list bendevcat` ne le liste pas. L'utilisateur confirme qu'il n'est **pas encore publié** et qu'il s'en charge. **Conséquence sur le contenu livré en T-A2, à ne pas oublier :** le champ `repoUrl` du skill est **délibérément absent** (le plan interdit un lien mort) et l'`installCmd` est publié **avec une mise en garde** dans le corps. Dès que le dépôt répond **200**, amender l'entrée `src/content/skills/anti-drift-planning/index.md` : ajouter `repoUrl`, remplacer la mise en garde par la vraie séquence d'installation (`git clone` compris), et re-vérifier le rendu de R6. **Tant que ce n'est pas fait, la clause « repo » du titre de R6 n'a rien à rendre** — sa mesure binaire (« corps rendu ET `installCmd` affiché et copiable ET `relatedPrompts` résolus ») reste, elle, entièrement atteignable.
- 2026-08-02 — **T-A2 → Done** — commits `f703288` (contenu) et `84c7885` (fix round 1). Livrés : 2 prompts réels (`bootstrap-session-anti-drift`, `format: fiche` · `decouper-un-projet-en-plans-anti-drift`, `format: guide`) et 1 skill réel (`anti-drift-planning`), avec la relation posée **dans les deux sens** (le skill pointe les 2 prompts, chaque prompt pointe le skill). **Vérifié par le contrôleur** : `vitest` **56/56**, `astro check` **0 error / 0 warning**, `astro build` **11 pages** sans aucun WARN `glob-loader`, arbre propre (aucun reliquat de sonde).
  - **Preuve R7 faite pour de bon** : sonde temporaire (correctement nommée, sans underscore initial — l'erreur du Plan 3, D04) → **4 lignes**, les deux sens, chacune avec l'id **et** le titre de la cible, **aucun `undefined`** ; puis l'échec a été **prouvé** — id cassé → `[ERROR] anti-drift-planning référence prompts → "nexiste-pas", introuvable.`, **exit 1, zéro page** ; puis restauration, re-build, suppression de la sonde.
  - **Revue de tâche : spec ✅ / qualité Changes requested** → 0 Critical, **4 Important**, 4 Minor. La revue portait sur **la véracité du contenu**, une vitrine publique n'ayant pas le droit d'inventer. Le relecteur a **recompté lui-même** chaque chiffre publié (27 déviations = 6+12+9, 3 rejetées, 9/7/7 critères tous Done, 7 commandes, 13 contrôles du linter, tags `archive/v0.4.0-*`) : **tous justes**. Et la transcription du champ `prompt` est **identique octet à octet** à `docs/anti-drift/handoffs/_next-session-prompt.md` — **10 952 octets**, établi par deux méthodes indépendantes, puis re-vérifié une troisième fois après correction. Aucune affirmation publiée sans source primaire relue.
  - Les 4 Important, tous **ADDRESSED** au round 1 : **I1** deux « ci-dessus » couplaient la prose à un ordre de rendu qu'aucun gabarit n'implémente encore (T-B3/T-C2 arrivent après) — reformulé sans aucun terme positionnel ; **I2** le prompt publié porte une coquille réelle (il demande de taguer `milestone-plan-3`, tag qui existe déjà ici) — le champ reste **verbatim**, mais le corps le dit désormais au lecteur ; **I3** la mise en garde sur `installCmd` ne nommait pas l'étape réellement bloquée, le `git clone <this-repo>` de `README.md:38` — nommée ; **I4** le guide publiait la substance de D05, périssable par construction — réécrit de façon intemporelle.
  - **`repoUrl` délibérément ABSENT** : `https://github.com/bendevcat/anti-drift-planning` répond **404**, re-mesuré sur trois canaux par l'implémenteur puis deux fois par les relecteurs. L'utilisateur confirme que le dépôt n'est pas encore publié. Le plan interdit d'écrire un lien mort ; l'`installCmd` est conservé (arbitrage utilisateur) avec la mise en garde.
  - **Défaut de process du contrôleur, reporté tel quel** : le paquet de re-revue a été généré sur la plage **inversée** `cdb25ed..84c7885` — vide, `cdb25ed` ayant atterri **14 secondes après** le commit de correction. Le relecteur l'a détecté, a recalculé `f703288..84c7885` et a relu le bon diff **au lieu de valider une plage vide**. Cause : j'ai lu `git log` comme une liste chronologique ascendante alors qu'elle est descendante. **Règle pour la suite : contrôler l'ancestralité (`git merge-base --is-ancestor`) avant de générer un paquet de revue.** C'est la même famille d'erreur que celle du Plan 3 (décrire l'arbre de mémoire au lieu de le lire).
  - **4 Minor déférés** à la revue finale de branche (chemin d'installation fictif pour le lecteur, 2 affirmations contrefactuelles non sourçables, comptes de mots du rapport sous-évalués, « 5 verrous » de la fiche vs « 4 verrous » de la méthodologie de ce dépôt — les deux exacts dans leur cadre mais dissonants côte à côte).
  - **R1 et R7 restent In progress.** R1 : 2 prompts ✅ mais **1 seul skill** au lieu de ≥ 2 (**D05**, `pending-user`). R7 : les données sont en place **et prouvées** dans les deux sens, mais son critère parle de **rendu** (« un skill liste ses prompts liés », « navigation croisée ») → T-C2. **T-B1 est débloquée** : 2 prompts, 2 formats distincts (`fiche`, `guide`).

- 2026-08-02 — T-B1 (carte prompt + grille `/prompts`) → **In progress** — R2 et R9 In progress.
- 2026-08-02 — **T-B1 → Done** — commit `ada7349`. `src/components/PromptCard.astro` (variante de `ProjectCard.astro`, porte le contrat `data-facet-card` / `data-facet` que T-B2 et T-C1 consomment) et `src/pages/prompts/index.astro` (grille + état vide, calquée sur `/projets`). **Vérifié par le contrôleur** : `vitest` **56/56**, `astro check` **0 error / 0 warning** (57 hints), `astro build` **12 pages** (11 + `/prompts`).
  - **Smoke navigateur fait par le contrôleur** (hors périmètre du subagent) : `/prompts` rend **2 cartes**, chacune avec format, outil, titre, description et tags ; le `data-facet` de chaque carte se `JSON.parse` et contient bien ses 3 clés `format`/`tool`/`tag`, toutes en tableaux ; l'état vide est masqué ; **aucune barre de filtres** (correcte absence — elle arrive en T-B2) ; ordre alphabétique respecté. À **375px** : `scrollWidth == innerWidth == 375` et **zéro** élément plus large que le viewport, en **dark et en light**.
  - **Correction d'une fausse alerte du contrôleur, reportée telle quelle** : une première mesure en thème clair montrait la carte avec le fond **sombre** `#141B24`. Vérification faite : c'était un **artefact de mesure** — le thème avait été basculé en cours de page sans rechargement. Après un vrai rechargement en clair, la carte est bien `#FFFFFF` avec la bordure claire, et `/projets` (livré au Plan 3, mêmes classes) se comporte à l'identique. **Aucun défaut, aucune correction faite.** Consigné parce qu'une mesure erronée qui disparaît sans trace est le début d'un fait établi de travers.
  - **Revue de tâche : spec ✅ / qualité Approved** — **0 Critical, 0 Important**, 3 Minor déférés. Le relecteur a fait la comparaison **ligne à ligne** `PromptCard.astro` vs `ProjectCard.astro` : tous les blocs communs sont des copies exactes, et les seuls écarts (pas d'image, 2 puces au lieu d'1, `aria-label` différent) découlent de la différence de schéma. Rien d'anticipé sur T-B2 ni T-B3.
  - **R2 et R9 restent In progress** : R2 exige aussi le filtre (T-B2), R9 couvre aussi les pages de détail (T-B3, T-C1, T-C2).
- 2026-08-02 — T-B2 (moteur de filtrage à facettes + filtres de `/prompts`) → **In progress**.
- 2026-08-02 — **T-B2 → Done** — commits `0a17b0f` (implémentation) et `ea8a7e3` (fix round 1). `src/lib/facetFilters.ts` (moteur à facettes **pur, sans aucun import** — c'est ce qui lui permet d'être chargé par le navigateur ET par vitest), `src/lib/facetFilters.test.ts` (9 cas), `src/scripts/facet-filters.ts` (script DOM générique, servira aussi `/skills`), la barre de filtres de `/prompts` (boutons `format`, `<select>` `tool` et `tag`), et `src/lib/projectFilters.ts` devenu un **adaptateur** — il n'y a plus qu'un seul moteur de filtrage dans le dépôt. **Vérifié par le contrôleur** : `vitest` **65/65**, `astro check` **0 error / 0 warning**, `astro build` **12 pages**.
  - **Preuve de non-régression, le point central de cette tâche** : `src/lib/projectFilters.test.ts` (Plan 3) est resté **intact à la ligne près** — `git diff --stat` vide, re-vérifié après le fix round. C'est ce qui établit que l'adaptateur est fidèle. Le relecteur ne s'en est pas contenté : il a **déplié `matchesFacets` cas par cas** sur 8 scénarios (facette absente, tableau vide, sentinelle en sélection, sentinelle **en valeur d'entrée**, `[x].includes` vs `===`, court-circuit, clés surnuméraires, `undefined`) — **aucune divergence** sur le domaine typé. Les 2 seuls écarts trouvés sont hors types, inatteignables depuis `project-filters.ts`, et vont vers la robustesse.
  - **Le script DOM du Plan 3 n'a PAS été migré** (décision de plan ratifiée au gate) : `src/scripts/project-filters.ts` est **absent du diffstat**. Deux scripts DOM coexistent au-dessus d'un moteur unique — résiduel assumé, pas une dérive.
  - **Smoke navigateur fait par le contrôleur** — **clause binaire de R2 vérifiée par COMPTAGE**, pas à l'œil : état initial 2 cartes (`guide` + `fiche`) ; `format=fiche` → **1 visible, aucun `guide`** ; `format=guide` → **1 visible, aucune `fiche`** ; combinaison **ET** `guide` + `tag=prompt-engineering` → **0 visible, état vide affiché** ; retour « tous » → 2 visibles, état vide masqué ; `aria-pressed` suit la sélection. **Sans JS** (HTML servi par le serveur, avant tout script) : barre présente **avec `hidden`**, **2 cartes toutes visibles**, aucun contrôle mort — l'enhancement progressif est réel, pas déclaratif. À **375px, dark et light** après rechargement réel dans chaque thème : `scrollWidth == innerWidth == 375`, zéro débordement, `<select>` et boutons correctement thémés.
  - **Revue de tâche : spec ✅ (13/13) / qualité Changes requested** → 0 Critical, **1 Important**, 5 Minor. **I1** : `(entry[key] ?? []).includes(value)` ne vérifiait jamais que la valeur de facette est un **tableau** — sur une chaîne, `.includes` devient un **matching de sous-chaîne** (filtrer sur `fic` matcherait `fiche`), et le `JSON.parse` du script est **casté** sans garde, donc TypeScript est aveugle. **Non atteignable aujourd'hui** (`PromptCard` sérialise bien des tableaux), mais le module est **livré tel quel à T-C1** : le corriger maintenant évite qu'un contrat implicite casse à la tâche suivante, entre deux implémenteurs qui ne se sont pas lus. **Déviation D06 loggée avant le correctif.** Fix round 1 → **ADDRESSED**, test prouvé **discriminant** (avec l'ancienne implémentation il échouerait), aucun durcissement excédentaire, aucune régression.
  - **Jugement du contrôleur consigné, pour qu'il soit auditable : l'élargissement de la règle `[hidden]` de `src/styles/global.css` n'est PAS une déviation.** Le Step 8 du plan prescrit explicitement la branche « si elle est restreinte, l'élargir ». L'implémenteur l'avait signalée par prudence. Vérifié : l'élargissement est **minimal et additif** (3 sélecteurs `data-facet-*` ajoutés au bloc existant), les 3 sélecteurs `data-project-*` sont inchangés au caractère près — `/projets` ne peut pas avoir bougé. Le brief se contredisait sur ce fichier (table « read-only » vs Step 8) ; sans cet élargissement, la fonctionnalité serait cassée **avec des tests verts**.
  - **5 Minor déférés** à la revue finale de branche (`JSON.parse` renvoyant `null`, clé de facette homonyme d'une propriété d'`Object.prototype`, trous résiduels de la matrice, asymétrie facettes statiques/dérivées pouvant laisser un filtre mort, placement du `<script>`).
  - **R2 → Done.** **R9 reste In progress** : il couvre aussi les pages de détail (T-B3, T-C1, T-C2).
- 2026-08-02 — T-B3 (`/prompts/<slug>` — fiche vs guide, copie 1 clic, fallback presse-papier) → **In progress** — R3 et R4 In progress.
- 2026-08-02 — **T-B3 → Done** — commits `c6c3514` (implémentation), `9f26c6f` (correctif D07) et `354d01b` (fix round 1). `src/lib/clipboard.ts` + tests (6 cas), `src/lib/promptView.ts` + tests (3 cas), `src/pages/prompts/[...slug].astro` (fiche vs guide, bloc copiable, skills liés — addition n°2 ratifiée), et `src/scripts/copy-code.ts` qui gagne le **fallback** exigé par la spec §6.1 et absent depuis le Plan 1. **Vérifié par le contrôleur** : `vitest` **74/74** (9 fichiers), `astro check` **0 error / 0 warning** (58 hints), `astro build` **14 pages**, `src/styles/global.css` **byte-identique**.
  - **R3 prouvé au navigateur, sur le contenu et pas sur le libellé** : le texte transmis au presse-papier est **identique au bloc rendu** (**10 579 caractères**), il ne contient **pas** le libellé du bouton (le piège du Plan 1 est évité), et le feedback passe par « Copié ! » puis revient à « Copier » après 1,5 s. Le presse-papier réel est bloqué par le contexte d'automatisation (`NotAllowedError`) : ce qui est établi, c'est **ce que le code transmet à l'API** et les deux chemins de feedback — pas le tampon système lui-même. **Le chemin d'échec a été observé réellement**, sans instrumentation : « Échec — copie manuelle ». **Non-régression du blog prouvée** sur un article à **42 blocs de code → 42 boutons**, tous ancrés dans leur wrapper, copie exacte.
  - **R4 prouvé** : la fiche rend le bloc ; le guide rend 6 sections (~6 200 caractères) et **zéro `<pre>`** dans l'article.
  - **DÉFAUT MAJEUR TROUVÉ AU SMOKE, invisible pour toute la chaîne automatisée** : le `<pre>` du prompt était rendu **hors de `.prose`**, où vit `overflow-x: auto`. Mesuré : `overflow-x: visible`, padding/bordure/rayon à **0**, `<code>` sur **3 946 px**, page à **3 961 px** → **2 461 px de débordement horizontal**, R9 cassée et spec §6.2 jamais honorée. **`astro build`, `astro check` et les 73 tests étaient tous VERTS.** Rien dans la chaîne ne mesure une largeur rendue. **Déviation D07 loggée avant correctif** ; après correctif, `scrollWidth == innerWidth == 375`, **écart 0 px**, bouton toujours cliquable après scroll horizontal complet du bloc.
  - **Revue de tâche : spec ✅ / qualité Changes requested** → 0 Critical, **3 Important**, 5 Minor. **I1** : la classe `astro-code` ajoutée pendant le correctif était justifiée par un raisonnement **faux** — le relecteur l'a établi sur le **CSS buildé** (une valeur héritée ne concurrence jamais une règle qui matche l'élément ; le preflight Tailwind v4 met déjà `pre` en JetBrains Mono ; et la classe attache en réalité **5 déclarations `!important` de Shiki**, bénignes **par accident**). Retirée, commentaire corrigé, police mono re-mesurée sans elle. **Cette forme allait être reprise verbatim pour T-C2** : c'est ce qui rend le constat important. **I2** : l'état d'échec n'était annoncé à aucun lecteur d'écran → `aria-live="polite"`. **I3** : cas de test manquant sur la branche « aucune dépendance ». **Déviation D08 loggée** pour I2 et I3, **addendum à D07** pour I1.
  - **RÉFUTATION établie, et elle va contre la revue** : l'implémenteur a démontré que le mutant invoqué par I3 (`legacyCopy?.() ?? false` → `legacyCopy!()`) est un **mutant équivalent** — le `try/catch` absorbe indifféremment l'exception d'un appel sur `undefined` et celle d'un `legacyCopy` réel. **Re-vérifié indépendamment par le contrôleur** en exécutant les deux formes sur **6 jeux d'entrées** : sortie **identique dans les 6 cas**. Le raisonnement du relecteur était donc faux ; le test ajouté reste utile (il fixe le contrat « ne lève jamais, renvoie `false` ») mais **pas** pour la raison invoquée. Aucune restructuration de `copyText` n'a été faite pour forcer un « kill » artificiel : ça aurait affaibli la garantie « ne lève jamais ».
  - **RÉSERVE OUVERTE, à porter au walkthrough de la Phase Z** : la re-revue a établi, sur l'arbre d'accessibilité du navigateur, que le **nom accessible** du bouton reste figé sur `aria-label="Copier le code"` et ne change jamais, même quand le texte passe à « Copié ! » ou « Échec — copie manuelle ». Selon la façon dont un lecteur d'écran réel construit l'annonce d'une région live, le correctif I2 **pourrait n'annoncer rien d'utile**. **Invérifiable sans VoiceOver ou NVDA réel** — la boucle de correction n'a pas été prolongée sur ce qui ne peut pas être mesuré ici. Effet secondaire noté : le retour à « Copier » après 1,5 s est lui aussi annoncé, sur tous les boutons du site. **À trancher par un test humain, pas par un agent.**
  - **5 Minor déférés** à la revue finale de branche, dont **2 liens morts `/skills/<id>/`** en production tant que T-C2 n'est pas livrée (les 2 prompts portent `relatedSkills`).
  - **R3 → Done. R4 → Done. R9 reste In progress** : il couvre aussi `/skills` et `/skills/<slug>` (T-C1, T-C2).
- 2026-08-02 — **Plan amendé AVANT exécution de T-C2** (donc pas une déviation) : le bloc `installCmd` du Step 1 passe de `<pre class="mt-4">` à `<div class="prose mt-4"><pre>…</pre></div>`, **forme établie et vérifiée en T-B3**. Sans cette enveloppe, T-C2 aurait reproduit à l'identique le débordement de 2 461 px de D07. Le commentaire du plan porte les deux points déjà tranchés : le label `<h2>` reste **hors** de `.prose`, et le `<pre>` ne porte **aucune** classe.
- 2026-08-02 — T-C1 (carte skill + grille `/skills` filtrable) → **In progress** — R5 In progress.
- 2026-08-02 — **T-C1 → Done (au sens de la tâche ; R5 reste In progress)** — commit `555a161`. `src/components/SkillCard.astro` et `src/pages/skills/index.astro`, jumeaux de `/prompts` avec **2 facettes** (`type`, `tag`) au lieu de 3. **Aucun nouveau JS, aucun nouveau module de filtrage, aucune règle CSS** — le moteur de T-B2 a servi tel quel, ce qui était l'objet même de sa généralisation. **Vérifié par le contrôleur** : `vitest` **74/74** (inchangé, aucun module logique nouveau), `astro check` **0 error / 0 warning**, `astro build` **15 pages**.
  - **Smoke navigateur fait par le contrôleur** : barre révélée, boutons `type` = `[tous, claude-code]` construits depuis `collectTypes()` (il n'existe **pas** de constante `SKILL_TYPES` — `type` est une `z.string()` libre), `<select tag>` avec les 4 tags, `data-facet` à 2 clés **toutes deux en tableaux**. **Filtre exercé sur l'ensemble vide** — impossible autrement avec une entrée unique, donc en injectant une option réelle : tag réellement absent → **0 carte + état vide affiché** ; tag porté → 1 carte, état vide masqué. **Sans JS** : barre `hidden`, carte visible. **`/prompts` intact** (2 cartes, 3 boutons `format`) — le script est partagé, une régression y aurait été invisible depuis `/skills`. **375px dark et light** : écart **0 px**.
  - **Erreur de méthode du contrôleur, corrigée en direct et reportée** : un premier essai de filtre « tag inexistant » affichait 1 carte visible et **semblait révéler un bug**. En réalité affecter à un `<select>` une valeur sans `<option>` correspondante laisse `value` à `''`, et le script retombe alors sur la sentinelle `__all__` — le test était invalide, pas le code. Refait avec une option réellement injectée, le comportement est correct.
  - **Revue de tâche : spec ✅ / qualité bonne** — **0 Critical, 0 Important**, 2 Minor. Comparaison **ligne à ligne** avec le jumeau : tous les écarts (2 facettes au lieu de 3, pas de `<select tool>`, badge `version`) sont justifiés par la différence de schéma ou prescrits verbatim. **Aucun skill inventé, aucune dégradation liée au cardinal N=1** — la grille, les filtres et l'état vide fonctionnent à N entrées, un second skill s'ajouterait sans une ligne de code.
  - **Constat vérifié indépendamment** : `dist/prompts/index.html` change **uniquement** par le nom du chunk JS, Vite factorisant le script partagé dès que 2 pages l'utilisent au lieu de l'inliner. Le relecteur l'a établi par **build comparatif** (skills retiré puis restauré) : même hash de contenu, donc byte-identique. Sans conséquence.
  - **R5 reste In progress** : sa mécanique de filtre est prouvée, mais sa clause **« ≥ 2 cartes » n'est pas satisfaite** — un seul skill publié (**D05**, `pending-user`). Ce n'est pas maquillé en `Done`.
- 2026-08-02 — T-C2 (`/skills/<slug>` — instructions, `installCmd` copiable, repo, prompts liés + activation de la nav) → **In progress** — R6 In progress.
- 2026-08-02 — **T-C2 → Done** — commits `f1d0da5` (implémentation) et `55a1737` (fix round 1, D09). `src/pages/skills/[...slug].astro` (corps, `installCmd` copiable, repo conditionnel, prompts liés) et `src/components/Header.astro` (**addition n°1** : liens « Prompts » et « Skills » activés, commentaire mis à jour). **Vérifié par le contrôleur** : `vitest` **74/74**, `astro check` **0 error / 0 warning**, `astro build` **16 pages**.
  - **R7 bouclé par des requêtes réelles, pas par lecture de code** : `/skills/anti-drift-planning/` → prompt (**200**) → retour au skill (**200**) ; le **second** prompt répond 200 et renvoie lui aussi au skill. Titres réels affichés des deux côtés, **aucun `undefined`**. Et l'**échec est prouvé** : id de `relatedPrompts` cassé → `astro build` en **exit 1** avec le message du garde-fou nommant le skill, la collection et l'id manquant ; restauré, arbre propre. C'est le premier moment du plan où cette protection est réelle — établi au Plan 3, une référence cassée laisse le build **vert** tant qu'aucune page ne la résout.
  - **R6 : `installCmd` copiable prouvé** — texte transmis **identique** au bloc rendu, sans le libellé du bouton, « Copié ! » affiché ; `<pre>` **dans `.prose` ET dans `<article>`**, `overflow-x: auto`, padding 14px, **aucune classe**. Corps rendu (7 sections). **Aucun bloc « code source »** : `repoUrl` est délibérément absent (dépôt en 404).
  - **R9 : écart 0 px à 375px sur les 4 pages livrées**, en dark **et** en light, bouton toujours cliquable après scroll horizontal complet du bloc, nav à 5 items tenant sur une ligne.
  - **Revue de tâche : conforme / Changes suggested** → 0 Critical, **2 Important**, 5 Minor. Le relecteur a établi lui-même que le fichier est **verbatim au brief** (diff programmatique, 0 ligne d'écart), que le `<pre>` est enfant direct de `.prose` dans l'`<article>` (offsets 2025 < 3222 < 11231), qu'**aucune URL de repli n'a été fabriquée**, qu'**aucun skill n'a été inventé**, et qu'il n'existe **aucun chemin de casse** sur les champs optionnels absents. Il a vérifié le commentaire de `Header.astro` **assertion par assertion** : les six sont vraies, « À propos » au Plan 5 étant doublement sourcé.
  - **I1** : le filtrage `draft` était réécrit à la main au lieu de composer les helpers **testés** dont le JSDoc nomme ce cas — seconde source de vérité, et **effet observable** (entrées liées dans l'ordre du frontmatter au lieu de A→Z). Corrigé **dans les deux sens** de la relation (T-B3 rouverte pour ne pas remplacer une incohérence par une asymétrie). **Déviation D09 loggée avant le correctif** ; ordre des opérations vérifié (garde-fou **d'abord**, helper **ensuite** — l'inverse aurait masqué une référence morte), et l'échec re-prouvé après correctif.
  - **I2 — constat qui vise le contrôleur, reporté tel quel** : le défaut « chiffre de baseline périmé » s'est **reproduit** dans le brief de T-C2 (« 68 tests », « M ≥ 2 pages ») sans être tracé. Les implémenteurs de T-B2, T-B3 et T-C2 ont chacun dû être avertis **à la main** dans leur dispatch ; le relecteur a fait remarquer que ces avertissements vivaient dans des messages, **pas dans le log** — « la Phase Z lit le log, pas les rapports ». C'est exact. **D02 a été élargie** en conséquence. Cause racine énoncée sans détour dans l'entrée : j'ai écrit dans le plan des chiffres que je n'avais pas mesurés.
  - **R7 → Done. R9 → Done.**
  - **R6 reste In progress — arbitrage rendu explicite plutôt que tranché à mon avantage.** Sa **mesure binaire** (spec §3 : « Corps rendu **ET** `installCmd` affiché et copiable **ET** `relatedPrompts` résolus ») est **entièrement satisfaite et prouvée**. Mais l'**intitulé** du critère nomme aussi « + repo », et ce bloc **n'a rien à rendre** tant que `https://github.com/bendevcat/anti-drift-planning` répond 404. Marquer `Done` reviendrait à valider « à peu près » un critère dont un élément nommé est absent — exactement ce que la méthodologie existe pour empêcher. **La Phase Z tranchera**, avec l'utilisateur : soit il publie le dépôt (l'entrée est alors amendée et R6 devient `Done` sans réserve), soit il acte que la mesure binaire fait foi.
- 2026-08-02 — T-D1 (collections `prompts` + `skills` dans le `config.yml` Sveltia + garde-fou de test) → **In progress** — R8 In progress.
- 2026-08-02 — **T-D1 → Done** — commits `6f256ce` (implémentation) puis `cc4b4cc`, `a7ec918`, `fee7389` (**3 fix rounds**). `public/admin/config.yml` gagne les collections `prompts` (10 champs) et `skills` (11 champs), `delete: false` sur les deux (**addition n°3**, ratifiée), et `src/lib/cms-config.test.ts` passe de **74 à 93 tests**. **Vérifié par le contrôleur** : `vitest` **93/93**, `astro check` **0 error / 0 warning** (66 hints), `astro build` **16 pages** (une config CMS ne produit aucune page).
  - **Smoke `/admin` fait par le contrôleur dans un vrai navigateur** : écran de connexion **normal** (3 options d'authentification), **aucun écran d'erreur** — une config invalide en afficherait un à la place — et **0 erreur console** (2 `info` de schéma JSON, 2 `warn` « 0.177.0 disponible », l'épinglage à `0.175.1` étant délibéré).
  - **Revue de tâche : conforme / 0 Critical, 4 Important, 8 Minor.** Le relecteur a **refait lui-même** le tableau champ par champ contre `src/content.config.ts` : mapping **exact, 9/9 et 10/10**, aucun champ en trop, required-ness toujours dans la direction sûre ; les **5 hints sont tous vrais** ; le README dit vrai.
  - **LA TROUVAILLE DE CE PLAN — 4ᵉ occurrence du mode de panne récurrent, et elle était vivante.** Question posée au relecteur : *ce projet a un mode de panne qui s'est produit trois fois — le CMS accepte, commite, et `astro build` casse APRÈS, en production. Cherche une quatrième occurrence.* Il l'a trouvée : le motif de `repoUrl` acceptait `https://github.com:bendevcat/…` (un `:` au lieu d'un `/` — faute de collage ordinaire), `http://exemple.fr:99999`, `https://exemple.fr:abc`, `http://[` — toutes **rejetées par Zod**, donc build interrompu et **0 page**. Pire : **le test censé l'interdire s'intitulait « n'est JAMAIS plus laxiste que Zod » et était faux**, ses 10 cas étant choisis à la main sans aucun port ni hôte invalide. **Et le Plan 3 avait déjà relevé ce défaut puis l'avait déféré « à trancher à la revue finale de branche » — ça n'a jamais été fait, et le Plan 4 l'a propagé à une troisième URL.** **Déviation D10 loggée avant correctif.**
  - **Trois rounds ont été nécessaires, et la raison vaut d'être retenue.** Round 1 : motif resserré sur les ports, cas choisis à la main remplacés par **4 095 cas générés** — vrai progrès, prouvé (756 de ces cas prennent l'ancien motif en défaut). Round 2 : le relecteur trouve **11 contre-exemples de plus** (`@ ? # % | < ^` non exclus de la classe d'hôte) — *une génération par produit cartésien ne couvre que l'espace que ses listes décrivent*, aucun hôte listé ne contenait ces caractères et tous les chemins commençaient par `/`. Round 3 : le contrôleur trouve **une dernière famille** — un hôte contenant un **octet NUL**, `\s` en regex ne couvrant pas les caractères de contrôle.
  - **État final, établi par trois balayages indépendants** (implémenteur 21 600 cas, contrôleur **10 416** avec ses propres familles dont les 34 caractères de contrôle, relecteur **13 160** hors des précédents) : **0 cas accepté par le motif et rejeté par Zod**. Un seul motif sur les 3 champs. Les URL réelles et les ports courants passent. L'écart **délibérément plus strict** que Zod (IPv6 littéraux, userinfo, port vide, ports à zéros, `ftp://`) est désormais **encodé explicitement dans un test**, au lieu d'être tacite.
  - **Portée sur du Plan 3, assumée** : `projects.repoUrl` et `projects.demoUrl` sont corrigés eux aussi. **Effet observable** : le CMS refuse désormais des URL qu'il acceptait — toutes invalides pour Zod, donc toutes vouées à casser le déploiement.
  - **Trois autres asymétries du garde-fou fermées** (I2, I3, I4) : assertions de `required`, de `widget`, et des clés `extension`/`format`/`media_folder`/`public_folder` sur `skills` — toutes présentes sur `blog`/`projects`, absentes sur les 2 nouvelles collections. Chacune **prouvée par mutation** : le test échoue bien quand on casse la config.
  - **8 Minor déférés** à la revue finale de branche, dont trois trous résiduels du garde-fou (`multiple: true` sur `prompts.format`, sous-schéma `fields:` d'un `list`, et `slug`/`identifier_field`/`create` jamais assertionnés sur les 4 collections — préexistant).
  - **R8 reste In progress** : la config est livrée, testée et smoke-testée, mais la clause « créer via `/admin` produit des fichiers conformes » exige une **création réelle** → **T-D2, geste utilisateur**.
- 2026-08-02 — **Revue finale de branche : 0 Critical, 3 Important, 21 Minor transverses.** Contraintes dures **re-vérifiées par le relecteur, pas crues** : diff `package.json`/`package-lock.json` **vide** (0 dépendance ajoutée), aucun framework ni lib, schémas **9/9 et 10/10 verbatim** de la spec de design §3.3/§3.4, Sveltia toujours à `0.175.1`, aucun nouveau token couleur. **Les deux intrusions dans du code du Plan 3 sont prouvées sûres** : `projectFilters.test.ts` et `project-filters.ts` **absents du diffstat**, `/projets` rend toujours ses 2 cartes, l'élargissement CSS est purement additif, et le motif d'URL resserré **accepte toujours les 3 URL réellement publiées** — aucun contenu du Plan 3 invalidé. Le relecteur en a nommé une **troisième non identifiée jusque-là** : `aria-live` posé par `copy-code.ts` sur **tous** les boutons des 8 pages livrées (couvert par D08, mais c'est la seule modification à changer un comportement runtime ailleurs). Les jumeaux `prompts.ts`/`skills.ts` **n'ont pas divergé** (5 fonctions comparées ligne à ligne, identiques) — seuls leurs JSDoc diffèrent.
- 2026-08-02 — **CONSTAT T1 DE LA REVUE FINALE, QUI VISE LE CONTRÔLEUR, REPORTÉ TEL QUEL.** Ma liste de constats déférés (`progress.md` du workspace SDD) était **incomplète** : 25 lignes pour environ 40 constats. **Les 8 Minor de T-D1 n'y avaient aucune ligne**, 4 lacunes remontées en re-revue non plus, et les 5 de T-C2 étaient repliés en une seule ligne-pointeur. Conséquence mesurée par le relecteur : suivre ma propre consigne de tri aurait **abandonné 15 constats en silence** — dont les **3 trous résiduels du garde-fou CMS**, c'est-à-dire exactement la famille de défauts que D10 vient de fermer. **La source qui fait foi pour le tri est donc l'ensemble des fichiers `task-*-review.md` et `task-*-rereview-*.md`, pas `progress.md`.** C'est la même erreur de forme que D02 : une trace tenue au fil de l'eau que je n'ai pas maintenue complète, et qui donne l'illusion d'un inventaire exhaustif.
- 2026-08-02 — **Tri des constats déférés (fait par la revue finale, pas par le contrôleur) : 4 sans objet · 6 à corriger avant merge · 30 à reporter.** Les 4 « sans objet » ont été **prouvés** tels, dont **B2-M2 réfuté par exécution** — la garde de type ajoutée en D06 l'avait déjà fermé. Les 6 « avant merge » font l'objet de la vague de correction **D11**. Le relecteur signale **3 combinaisons aggravantes**, dont une qui compte : `D1-M1 + M2 + M8` et trois lacunes de re-revue décrivent **le même mode de panne que D10**, sur le fichier que D10 vient de sauver — et **deux d'entre elles ne figuraient dans aucune ligne déférée**.
- 2026-08-02 — **Vague de correction finale → Done** — commit `5763215`. Les 2 `Important` transverses et les 6 `Minor` triés « avant merge » sont traités ; les 30 constats « à reporter » sont **restés intacts** (vérifié). **Re-revue ciblée : 7 ADDRESSED, 1 non applicable** (A2-1, conditionnel à la publication du dépôt — **404 re-confirmé** par le relecteur, donc rien n'anticipe une publication qui n'a pas eu lieu). **T2 vérifié des deux côtés** : les deux gardes sont désormais **textuellement identiques** (`(x ?? '').trim().length > 0`) et se comportent pareil sur les 5 entrées testées — un alignement partiel aurait été pire que l'état initial, parce qu'il aurait eu l'air réglé. **Les 5 commentaires et hints modifiés ont été confrontés au code réel** et sont vrais : c'est le contrôle le plus important de ce plan, dont **trois défauts ont été des affirmations fausses**.
- 2026-08-02 — **FIN DE L'EXÉCUTION — état « prêt à shipper, décisions en attente ».** 8 tâches sur 9 `Done` ; **T-D2 reste au geste de l'utilisateur** (un agent ne peut ni fournir le PAT ni actionner le sélecteur de dossier natif). Mesures finales : `vitest` **93/93 (9 fichiers)**, `astro check` **0 error / 0 warning** (66 hints), `astro build` **16 pages**, lint anti-drift **13/13, exit 0**. Branche à **40 commits, non poussée**. **La Phase Z n'a PAS été lancée** : elle échouerait sur les **11 déviations `pending-user`**, et un tag posé sur des déviations non approuvées vaut moins que pas de tag du tout. Handoff écrit dans `plan-4-handoff.md`, avec la liste exacte des décisions attendues. `/anti-drift-planning:resume 4` régénère le prompt de reprise.

