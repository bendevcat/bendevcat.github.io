# Plan 3 — Scope Ledger

Last updated: 2026-07-31 (pré-flight)
Last updated by: main (contrôleur SDD)

## Requirements (extracted from spec §3 Success criteria)

| ID | Requirement | Status | Notes |
|---|---|---|---|
| R1 | Collection `projects` + schéma Zod (page bundles) — `astro build` valide le schéma **ET** ≥ 2 fiches réelles chargées | Done | T-A1 (schéma + helpers + tests) puis T-A2 (fiches réelles). Le schéma est repris **verbatim** de la spec de design §3.2 — pas de champ `draft` pour les projets. |
| R2 | `/projets` rend une grille de cartes (titre, desc, statut, stack) — la page rend ≥ 2 cartes affichant ces 4 champs | Done | T-B1. Carte = **variante** de `ArticleCard.astro` (spec §6.2), pas un nouveau design system. |
| R3 | Filtre par statut et par stack — choisir un statut → **seules** les fiches de ce statut restent visibles (idem pour une techno) | Done | T-B2. JS vanilla progressif (spec §6.1), aucun framework. Prédicat pur testé unitairement + vérification par comptage dans le navigateur. |
| R4 | `/projets/<slug>` rend la fiche (corps + repo + démo) — corps rendu **ET** liens `repoUrl`/`demoUrl` présents quand définis | Done | T-B3. |
| R5 | Relation bidirectionnelle `blog ↔ projects` résolue — liens cliquables, **aucun `undefined`** | Done | Trois tâches : T-A2 (données des deux côtés), T-B3 (projet → articles), T-B4 (article → projets). `relatedProjects` existe déjà dans le schéma blog depuis le Plan 1 (`src/content.config.ts:20`) mais n'a jamais été rendu. |
| R6 | Projets éditables via le CMS — collection ajoutée au `config.yml` → créer un projet via `/admin` produit un fichier conforme au schéma | Done | T-C1 (config + garde-fou de test) puis **T-C2, geste utilisateur** : un agent ne peut ni fournir le PAT ni actionner le sélecteur de dossier natif (établi au Plan 2). |
| R7 | Cartes/fiches en direction dark editorial-dev, responsive 375px — visuel cohérent avec le blog **ET** pas de scroll horizontal à 375px | Done | Contrôle 375px + dark/light dans T-B1, T-B2, T-B3 et T-B4 (pas de tâche dédiée : chaque tâche livrant du gabarit le vérifie). |

## Status legend
- **Done** — verified, criteria passed (link to commit SHA)
- **In progress** — actively being worked
- **Pending** — not yet started
- **Deferred** — moved to a later plan (must have an approved deviation)
- **Cut** — removed from scope (must have an approved deviation)

## Task index → requirement(s) covered

| Task | Phase | Covers |
|---|---|---|
| A1 | A | R1 (collection `projects`, schéma Zod, helpers d'ordre + tests) |
| A2 | A | R1 (≥ 2 fiches réelles), R5 (données des deux sens) |
| B1 | B | R2 (grille + carte), R7 |
| B2 | B | R3 (filtre statut/stack), R7 |
| B3 | B | R4 (fiche + repo/démo), R5 (projet → articles), R7 |
| B4 | B | R5 (article → projets), R7 · **+ addition n°1** (lien de nav « Projets ») |
| C1 | C | R6 (collection CMS + garde-fou de test) · **+ addition n°2** (`relatedProjects` mappé côté blog) |
| C2 | C | R6 (création réelle via `/admin` — **action utilisateur**) |
| Z1 | Z | Audit `/anti-drift-planning:verify 3` (couverture R1–R7) — **Done, verdict PASS** |

## Additions au-delà de la lettre de la spec (à ratifier au gate de pré-flight)

Aucune n'est exigée par un critère R. Elles sont listées ici pour être approuvées ou coupées **explicitement**, jamais glissées en silence.

1. **Lien de nav « Projets » activé** (T-B4) — `src/components/Header.astro` le rend aujourd'hui non cliquable. Sans cette activation, `/projets` n'est atteignable qu'en tapant l'URL.
2. **`relatedProjects` mappé dans la collection `blog` du CMS** (T-C1) — champ explicitement différé par le Plan 2 (« collection `projects` = Plan 3 », omission approuvée). Sans lui, le sens article → projets reste éditable uniquement à la main.

## Updates log

(Append a one-liner each time the ledger is updated, with the new status and the commit SHA.)

- 2026-07-31 — **Ledger seeded (pré-flight)** — 7 lignes `Pending`. Branche `plan-3-vitrine-projets` créée depuis `main` (`9793f6b`, Plan 2 vérifié PASS). Plan d'implémentation écrit (`docs/anti-drift/plans/2026-07-19-plan-3-vitrine-projets.md`), log de déviations créé. **En attente de validation utilisateur avant T-A1**, avec deux questions ouvertes : les données réelles des ≥ 2 fiches projets (que le plan ne peut pas inventer) et les deux additions ci-dessus.
- 2026-07-31 — **GATE DE PRÉ-FLIGHT — plan validé par l'utilisateur.** Réponses explicites transcrites : (1) « Validé, démarre T-A1 » ; (2) **addition n°1 approuvée** — le lien de nav « Projets » sera activé en T-B4 ; (3) **addition n°2 approuvée** — `relatedProjects` sera mappé dans la collection `blog` du CMS en T-C1 ; (4) fiches retenues pour T-A2 : **`bendevcat.github.io`** (ce site) et **`awesome-french-tech-rss-feeds`**. Les deux additions étant ratifiées **avant** exécution, ce ne sont pas des déviations.
- 2026-07-31 — T-A1 (collection `projects` + schéma Zod + helpers d'ordre + tests) → **In progress** — R1 In progress.
- 2026-07-31 — T-A1 → **Done** — commit `d3978aa`. `src/content.config.ts` gagne `PROJECT_STATUSES` et la collection `projects` (schéma **verbatim** de la spec de design §3.2, sans champ `draft`) ; `src/lib/projects.ts` (`sortProjects`, `collectStacks`, `getSortedProjects`) ; `src/lib/projects.test.ts` (7 tests). **Vérifié indépendamment par le contrôleur** : `vitest` **22/22** (15 préexistants + 7), `astro check` **0 error / 0 warning** (30 hints, contre 14 au Plan 2 — hints uniquement, aucun n'est bloquant), `astro build` **8 pages**. **Revue de tâche : spec ✅ / qualité Approved**, 0 Critical, 0 Important, **1 Minor déféré** (le commentaire JSDoc de `sortProjects` affirme que `localeCompare('fr')` « ignore casse et accents » — le relecteur a vérifié en Node que c'est faux : ce sont des critères de départage secondaires. Sans conséquence fonctionnelle ; à corriger au passage d'une tâche ultérieure ou à la revue finale). Le relecteur a confronté le schéma à la **spec de design elle-même**, pas au brief. **R1 reste In progress** : sa clause « ≥ 2 fiches réelles chargées » exige T-A2. Constat honnête reporté par l'implémenteur : le build émet `[WARN] [glob-loader] No files found matching …` tant que la collection est vide — attendu, disparaît en T-A2.
- 2026-07-31 — **Plan amendé AVANT exécution de T-A2** (donc pas une déviation — précédent Plan 2, version du CDN Sveltia amendée au gate). Le gabarit à trous de T-A2 est remplacé par les **deux fiches exactes**, chaque valeur étant un fait vérifié : (1) `site-bencat` — ce dépôt, `created_at` 2026-07-30, stack lue dans `package.json`, `https://bendevcat.github.io/` répond 200 (Plan 2), statut `wip` et article lié « Bienvenue dans mon foutoir ! » **choisis par l'utilisateur** ; (2) `resumexyz` — API GitHub : créé le 2017-02-01, dernier push 2017-02-05, stack HTML/CSS/PHP/JavaScript **citée par son propre README**, statut `archivé`. **Aucun `demoUrl` pour `resumexyz` : `benoitcatillon.xyz` ne résout plus** (vérifié, pas de réponse DNS) — mettre le lien aurait été un lien mort. Le 2ᵉ choix de l'utilisateur pour cette fiche est `resumexyz`, en remplacement de `awesome-french-tech-rss-feeds`, dont le dépôt s'est révélé **vide** (README de 31 octets contenant le seul titre) : rédiger une fiche dessus aurait exigé d'inventer.
- 2026-07-31 — T-A2 (≥ 2 fiches réelles + relations bidirectionnelles) → **In progress** — R1 et R5 In progress.
- 2026-07-31 — **CORRECTION de l'entrée ci-dessous (écrite dans `1625b29`), qui était FAUSSE.** Elle affirmait « T-A2 bloquée, **rien de commité** » et « la fiche `site-bencat` n'est pas créée non plus ». **Faits établis sur `git log` :** le subagent T-A2 avait **déjà commité `7edc67e`** — `src/content/projects/site-bencat/index.md`, `src/content/projects/resumexyz/index.md` et la ligne `relatedProjects: [site-bencat]` sur l'article « Bienvenue dans mon foutoir ! » — **avant** que l'interruption de l'utilisateur ne prenne effet. `7edc67e` est le **parent direct** de `1625b29`, le commit qui affirmait le contraire. L'erreur a été **relevée par le relecteur de T-C1**, qui l'a établie sur `git log` / `git show` / `git merge-base`, et non sur mes affirmations — c'est le contrôleur qui a écrit un état non vérifié. **Cause :** j'ai décrit l'arbre de mémoire au lieu de lire `git log` après une interruption. **Règle pour la suite du plan : lire `git log` avant d'écrire dans le ledger ce que le dépôt contient — une interruption arrête le dialogue, pas le travail déjà commité.** D01 a été corrigée en conséquence.
- 2026-07-31 — **État réel de T-A2 : partiellement livrée** (`7edc67e`). Livré et conservé : la fiche `site-bencat` (faits vérifiés : dépôt créé le 2026-07-30, stack lue dans `package.json`, `https://bendevcat.github.io/` en 200) **et la relation bidirectionnelle** (`relatedPosts: [bienvenue-dans-mon-foutoir]` côté projet, `relatedProjects: [site-bencat]` côté article). Retiré sur décision explicite de l'utilisateur : la fiche `resumexyz` (**déviation D02**, loggée avant exécution). **Il manque donc la 2ᵉ fiche réelle** exigée par R1 (« ≥ 2 fiches réelles chargées ») : elle viendra d'un projet que l'utilisateur doit d'abord publier. **R1 et R5 restent In progress ; T-B1 → T-B4 restent bloquées** (leurs critères exigent ≥ 2 cartes rendues).
- 2026-07-31 — ~~**T-A2 BLOQUÉE côté utilisateur, rien de commité.**~~ *(entrée fausse, conservée pour la traçabilité — voir la correction ci-dessus)* L'utilisateur retire `resumexyz` du périmètre : « je vais voir pour push quelques projets locaux pour avoir de vrais exemples ». La 2ᵉ fiche exigée par R1 dépend donc d'un push que seul lui peut faire. **La fiche `site-bencat` n'est pas créée non plus** : T-A2 livre les deux fiches et la relation d'un seul tenant, la découper serait un second écart pour rien. **T-B1 → T-B4 sont bloquées avec elle** (leurs critères se vérifient sur des cartes et des fiches rendues). **Déviation D01 loggée** (`pending-user`, réversibilité `cheap`) avant exécution : T-C1 passe devant, c'est la seule tâche du plan indépendante de tout contenu. L'amendement du plan pour `resumexyz` (commit `3b52fa0`) devient caduc et sera réécrit avec les vrais projets avant que T-A2 reprenne.
- 2026-07-31 — T-C1 (collection `projects` dans le `config.yml` Sveltia + garde-fou de test) → **In progress** — R6 In progress.
- 2026-07-31 — T-C1 → **Done** — commit `5500325` + correctif `91498c1`. `public/admin/config.yml` gagne la collection `projects` (13 champs, médias **entry-relative** comme `blog`, `choose_url: false` sur `cover`, widget `relation` vers `blog` pour `relatedPosts`) et le champ `relatedProjects` sur la collection `blog` (addition n°2, ratifiée au gate). `src/lib/cms-config.test.ts` passe de 14 à 25 tests. **Vérifié indépendamment par le contrôleur** : `vitest` **33/33**, `astro check` **0 error / 0 warning** (33 hints), `astro build` **8 pages**. **Smoke `/admin` fait par le contrôleur dans un vrai navigateur Chromium** (Step 5bis, hors périmètre du subagent) : `http://localhost:4321/admin/index.html` affiche l'écran de connexion normal avec **0 erreur console** — seulement 2 `info` (suggestion de schéma JSON) et 2 `warn` (« version 0.176.0 disponible » : l'épinglage à 0.175.1 est délibéré). Une config invalide aurait affiché l'écran d'erreur à la place.
  - **Revue de tâche : spec ✅ / qualité Changes requested** → **1 Important**, corrigé en 1 round. Le relecteur a établi sur le **bundle épinglé** (`_A(n[0]).test(String(i))`, `RegExp.test` **non ancré à droite**) puis reproduit en Node que `pattern: '^https?://'` laissait passer `https://`, `http://`, `https:///` et `http://exa mple.com` — que `z.string().url()` rejette. Scénario complet : le CMS valide et commite, puis `astro build` casse le déploiement **après coup**. Le défaut venait du **plan** (YAML imposé verbatim), pas de l'implémenteur. **Déviation D03 loggée avant le correctif.**
  - **Correctif `91498c1`** : motif ancré des deux côtés `^https?://[^\s/]+(/[^\s]*)?$`, établi **empiriquement** contre le vrai `z.string().url()`, plus un test qui lit le motif **depuis le YAML** et le confronte à Zod sur 10 cas — au lieu de le comparer à des exemples choisis. **Re-revue ciblée : ADDRESSED**, prouvée par exécution Node et par mutation en mémoire (avec l'ancien motif, le test échoue sur les 4 cas — il n'est donc pas une coquille). **Aucun nouveau défaut**, aucune dépendance ajoutée (`z` importé depuis `astro:content`).
  - **Écart assumé et documenté** : le motif refuse `ftp://exemple.fr`, que Zod accepterait. Volontaire — le motif est **plus strict** que Zod sur le schéma d'URL, jamais plus laxiste. Encodé explicitement dans le test plutôt que masqué.
  - **1 Minor déféré** (hors périmètre du correctif, **préexistant**, pas une régression) : `https://exemple.fr:99999` passe le motif mais est rejeté par Zod (port hors plage). L'ancien motif l'acceptait déjà. Corrigeable par `[^\s/:]+(:\d{1,5})?` si on veut fermer complètement la classe « motif plus laxiste que Zod ». À trancher à la revue finale de branche.
  - **R6 reste In progress** : la config est livrée et testée, mais la clause « créer un projet via `/admin` produit un fichier conforme au schéma » exige une création réelle → **T-C2, geste utilisateur**, qui a besoin que la collection ait des entrées.
- 2026-07-31 — **Plan ré-amendé AVANT reprise de T-A2** (donc pas une déviation). L'utilisateur fournit **`https://github.com/bencatlab/gha-svu`** comme 2ᵉ projet, en attendant d'en publier d'autres. Faits établis **sur la source** : API GitHub (`created_at` 2025-05-05, `pushed_at` 2026-05-13, tags `v0.0.1`→`v0.0.3`, **ni `homepage` ni `license`**), `README.md` et `action.yml` du dépôt. **Aucun `demoUrl`** : la fiche Marketplace `https://github.com/marketplace/actions/gha-svu` répond **404** (vérifié) — inventer ce lien aurait produit un lien mort. Statut `actif` (dernier push il y a ~2,5 mois, 3 versions publiées) : **jugement du contrôleur**, pas un fait — une ligne à changer si l'utilisateur voit les choses autrement. Le Step 1 (fiche `site-bencat`) est marqué **déjà livré** dans le plan pour que l'implémenteur ne le refasse pas. Le Step 3 gagne une **seconde paire bidirectionnelle** (`comment-jutilise-github-actions-au-quotidien` ↔ `gha-svu`), pour que R5 ne repose pas sur un cas unique. Le Step 4 est corrigé : le build reste à **8 pages** (aucune route projet avant T-B1/T-B3) et exige désormais de **prouver l'échec de la validation** en cassant un id volontairement.
- 2026-07-31 — T-A2 (reprise, 2ᵉ fiche) → **In progress**.
- 2026-07-31 — **T-A2 : l'implémenteur s'est ARRÊTÉ avant de committer, et il a eu raison.** Il a livré les Steps 2 et 3 dans l'arbre (fiche `gha-svu` conforme au brief au caractère près, une seule ligne ajoutée à l'article), puis a remonté un **constat majeur** : la méthode de vérification prescrite par le plan pour R5 était **factuellement fausse**, sur deux points. **Re-vérifié indépendamment par le contrôleur en trois builds réels**, pas cru sur parole : (1) un id cassé dans `relatedPosts`, **sans page qui le résout**, laisse le build **vert (exit 0, 8 pages)** — `reference()` ne valide que la forme, l'existence n'est contrôlée qu'à la résolution ; (2) la sonde prescrite, `src/pages/_probe-refs.astro`, ne pouvait **jamais** produire de page — Astro exclut du routage les fichiers préfixés par `_`, donc le `cat` du plan échouait même avec des données correctes. Avec une sonde correctement nommée : un id cassé fait bien **échouer** le build (`Entry blog → nexiste-pas was not found.`), et avec les bons ids la sortie montre les **deux paires** résolues avec leurs titres, **aucun `undefined`**. **Déviation D04 loggée** avant correction ; plan corrigé (Steps 4 et 5 réécrits, sonde renommée et étendue au sens article → projet).
- 2026-07-31 — **Conséquence à retenir jusqu'à la Phase Z** : tant que T-B3 et T-B4 ne sont pas livrées, **rien ne protège d'une référence pointant dans le vide** — le build reste vert. À partir de T-B4 la protection existe et elle est bruyante (build rouge). Ce fait doit être re-vérifié au walkthrough de R5, pas supposé.
- 2026-07-31 — **T-A2 → Done** — commits `7edc67e` (fiche `site-bencat` + 1ʳᵉ relation) et `708065b` (fiche `gha-svu` + 2ᵉ relation). **Vérifié par le contrôleur** : `vitest` **33/33**, `astro check` **0 error / 0 warning**, `astro build` **8 pages**, arbre **propre** (aucun reliquat de sonde). Step 5 exécuté avec la sonde corrigée : **4 lignes**, chacune avec l'id **et le titre** de la cible, **aucun `undefined`** ; et l'échec a été **prouvé pour de bon** dans les deux sens — `Entry projects → nexiste-pas was not found.` puis `Entry blog → nexiste-pas was not found.` + `TypeError` en sortie 1, avant restauration.
  - **Revue de tâche : spec ✅ / qualité Approved — 0 Critical, 0 Important, 0 Minor.** La revue portait sur **la véracité du contenu**, une vitrine publique n'ayant pas le droit d'inventer : chaque valeur de frontmatter et chaque affirmation des corps a été confrontée aux **sources primaires** — API GitHub (`created_at 2025-05-05`, tags `v0.0.1`→`v0.0.3`, `homepage: null`), `action.yml` (12 entrées, 3 sorties, 6 sous-commandes, garde-fou `MAJOR < 3`, commentaire sur `--always`/`--v0`), les 2 workflows, la Marketplace en **404**, et pour `site-bencat` le dépôt local (`package.json`, `deploy.yml`, `content.config.ts`) plus **le site en production** (nav « Projets/Prompts/Skills » encore grisée « Bientôt disponible » — l'affirmation « la vitrine est en cours » est donc exacte au moment où elle est écrite). Même « ce site remplace mon ancien blog Hugo » a été confirmé sur l'historique git (`6576f8e`, migration Plan 1).
  - **La fiche `site-bencat` a été relue à cette occasion** : commitée pendant l'interruption de session, elle n'était jamais passée par une revue. Elle est conforme et exacte.
  - **R1 → Done** : 2 fiches réelles chargées, schéma validé au build. **R5 reste In progress** : les données sont en place dans les deux sens, mais son critère parle de **rendu** (« liste ses articles », « liens cliquables ») → T-B3 et T-B4. **T-B1 → T-B4 sont débloquées** : 2 cartes, 2 statuts distincts (`wip`, `actif`).
- 2026-07-31 — T-B1 (carte projet + grille `/projets`) → **In progress** — R2 et R7 In progress.
- 2026-07-31 — **T-B1 → Done** — commit `2b119a1`. `src/lib/projectStatus.ts` (libellé + classes de puce, même pattern que `aiUsage.ts`), `src/components/ProjectCard.astro` (variante d'`ArticleCard.astro`, porte le contrat `data-project-card` / `data-status` / `data-stack` que T-B2 consommera), `src/pages/projets/index.astro` (calquée sur `/blog`). **Vérifié par le contrôleur** : `vitest` **33/33**, `astro check` **0 error / 0 warning**, `astro build` **9 pages**.
  - **Smoke navigateur fait par le contrôleur** (hors périmètre du subagent) : `/projets` rend **2 cartes** avec, pour chacune, les **4 champs de R2** — titre, description, puce de statut (`wip` ambre, `actif` vert) et puces de stack (5 et 4 technos) — et les `href` corrects vers `/projets/site-bencat/` et `/projets/gha-svu/`. Contrôlé en **dark** et en **light**. À **375px** : `scrollWidth == innerWidth == 375`, et la liste des éléments dépassant la largeur du viewport est **vide**.
  - **Revue de tâche : spec ✅ / qualité Approved**, 0 Critical, 0 Important, **2 Minor déférés** : (1) la puce de statut porte une `border` que la puce de catégorie du blog n'a pas — écart R7 mineur, hérité du code du plan, défendable pour `archivé` (sans bordure elle serait invisible sur `--color-surface`) mais pas nécessaire pour `actif`/`wip` ; (2) le type `ProjectStatus` est retapé à la main dans `projectStatus.ts` au lieu d'être dérivé de `PROJECT_STATUSES` — deux sources de vérité, mais le relecteur a **prouvé empiriquement** qu'un statut ajouté au schéma sans métadonnée fait échouer `astro check` (`ts(7053)`), donc la régression ne peut pas passer silencieusement.
  - **Le relecteur a validé le contrat `data-stack` sur une valeur hostile** plutôt qu'en théorie : une techno contenant apostrophes, guillemets, chevrons, `<script>` et `&` est correctement échappée par Astro et se `JSON.parse` sans erreur côté navigateur. Aucune saisie libre du CMS ne peut casser le filtre de T-B2.
  - **R2 → Done. R7 reste In progress** : il couvre aussi les fiches, livrées en T-B3.
- 2026-07-31 — T-B2 (filtre statut + stack, JS vanilla progressif) → **In progress** — R3 In progress.
- 2026-07-31 — **T-B2 → Done** — commit `ce32169`. `src/lib/projectFilters.ts` (prédicat **pur**, zéro import), `src/lib/projectFilters.test.ts` (5 tests), `src/scripts/project-filters.ts` (enhancement navigateur), barre de filtres dans `src/pages/projets/index.astro`, règle de masquage dans `src/styles/global.css`. **Vérifié par le contrôleur** : `vitest` **38/38**, `astro check` **0 error / 0 warning**, `astro build` **9 pages** (un filtre ne crée aucune route).
  - **R3 mesuré dans un vrai navigateur par le contrôleur**, pas déduit du HTML : barre révélée par le script avec `tous` en `aria-pressed="true"` ; `<select>` contenant `toutes` + les **9** technos (union exacte des deux fiches) ; `wip` → **seule** `site-bencat` ; `actif` → **seule** `gha-svu` ; `archivé` → **0 carte et l'état vide s'affiche** ; techno `Astro` → `site-bencat` seule, `Go` → `gha-svu` seule ; combinaison `actif` + `Astro` → **0 carte + état vide**, ce qui prouve la sémantique **ET** ; retour à `tous`/`toutes` → les 2 cartes reviennent. Le cas `archivé` est un test gratuit offert par les données réelles : aucune fiche ne porte ce statut.
  - **Progressivité prouvée sur le HTML servi** : la barre et l'état vide sortent du serveur avec l'attribut `hidden`, **les 2 cartes sans `hidden`**, et la règle `display:none!important` est bien dans le CSS construit. Sans JS : aucun contrôle mort affiché, toutes les cartes visibles.
  - **375px (R7)** : `scrollWidth == innerWidth == 375`, aucun élément débordant, barre passée en colonne. Contrôlé en dark et en light.
  - **Revue de tâche : spec ✅ / qualité Approved**, 0 Critical, 0 Important, **1 Minor déféré** : le `try/catch` autour de `JSON.parse(data-stack)` rattrape une erreur de syntaxe mais pas un JSON *valide de mauvais type* (`"null"`, `"5"`) — `entry.stack.includes()` lèverait alors une `TypeError` non rattrapée qui laisserait la barre visible mais désynchronisée. Risque très faible (le seul producteur est `JSON.stringify(stack)` sur une valeur garantie tableau par Zod) ; parade d'une ligne : `Array.isArray(stack) ? stack : []`.
  - Le relecteur a **vérifié la pureté du module sur le bundle construit**, pas seulement sur la source : aucune trace de logique de collection dans le JS envoyé au navigateur.
- 2026-07-31 — T-B3 (fiche `/projets/<slug>` : corps, repo/démo, articles liés) → **In progress** — R4 et R5 In progress.
- 2026-07-31 — **T-B3 → Done** — commit `3edc4eb` + correctif `4e5e44a`. `src/pages/projets/[...slug].astro` (124 lignes, calquée sur la page article). **Vérifié par le contrôleur** : `vitest` **40/40**, `astro check` **0 error / 0 warning**, `astro build` **11 pages** (9 + les 2 fiches), `grep -ri undefined dist/projets/` → **aucune occurrence**, arbre propre.
  - **R4 mesuré dans un vrai navigateur** : `/projets/site-bencat/` rend le corps (943 caractères, 2 `<h2>`), la puce `wip`, « depuis juillet 2026 », les 5 puces de stack, et **les deux** boutons — `code source ↗` vers le dépôt et `démo ↗` vers le site, tous deux en `rel="noopener noreferrer"`. `/projets/gha-svu/` rend **un seul** bouton : pas de `demoUrl`, donc **pas de bouton mort**. **R5, sens projet → articles** : la section `// articles liés` porte le **titre** de l'article et un `href` valide, sur les deux fiches. **375px** en light : `scrollWidth == innerWidth == 375`, aucun élément débordant.
  - **Revue de tâche : spec ✅ / qualité Approved**, 0 Critical, **1 Important** corrigé en 1 round, 3 Minor déférés (écarts de marge assumés vis-à-vis de la page article ; pas d'annonce « nouvelle fenêtre » pour les lecteurs d'écran — convention déjà en place dans `Hero.astro`, pas une régression).
  - **Le constat Important, établi en cassant une vraie référence** : `getEntries` d'Astro n'échoue pas sur une référence introuvable — `createGetEntry` fait `console.warn(...)` puis `return`, donc **`undefined` entre dans le tableau**, traverse `sortAndFilter` et produit un `TypeError` anonyme qui **interrompt tout le build** (11 pages perdues, pas seulement la fiche fautive). La lettre de R5 était respectée (aucun `undefined` rendu, échec bruyant) mais l'échec n'était **pas diagnosticable**. **Déviation D06 loggée avant correctif.**
  - **Correctif `4e5e44a`** : helper générique `assertEntriesResolved` dans `src/lib/projects.ts`, qui **lève** en nommant l'entrée porteuse, la collection visée et **l'id manquant** — message réel mesuré : `_probe-broken référence blog → "cet-article-nexiste-pas", introuvable. Vérifie que l'entrée existe encore et que l'id est correctement orthographié.` Le filtrage silencieux a été **explicitement écarté** : un build vert obtenu en supprimant discrètement un lien est la perte silencieuse que ce plan existe pour empêcher. Un article `draft: true` référencé reste écarté **sans** erreur — le cas « draft » n'est pas confondu avec le cas « cassé ». +2 tests.
  - **Re-revue ciblée : ADDRESSED**, aucun nouveau défaut. Le relecteur a prouvé que les tests ne sont pas des coquilles par **mutation testing** : en remplaçant le helper par un filtre silencieux, le test échoue immédiatement. Il a aussi vérifié que la signature générique tient pour T-B4 (sens article → projets), sans spécialisation implicite à `blog`.
  - **R4 → Done. R5 reste In progress** (sens article → projets = T-B4). **R7 reste In progress** (T-B4).
- 2026-07-31 — T-B4 (sens article → projets + activation du lien de nav) → **In progress** — R5 et R7 In progress.
- 2026-07-31 — **T-B4 → Done** — commit `ee88072`. `src/pages/blog/[...slug].astro` rend la section « projets liés » **après** le corps (miroir exact de « articles liés » : mêmes classes, seuls le kicker et l'URL diffèrent), et `src/components/Header.astro` active le lien de nav « Projets » (**addition n°1**, ratifiée au gate). Le helper `assertEntriesResolved` de D06 est branché **des deux côtés** de la relation, avec `ownerId = post.id` — donc un message d'erreur nommerait bien l'**article** porteur, pas le projet. **Vérifié par le contrôleur** : `vitest` **40/40**, `astro check` **0 error / 0 warning**, `astro build` **11 pages**, arbre propre.
  - **La user story §1.4 est bouclée, mesurée par clics réels dans le navigateur** : depuis `/blog/comment-jutilise-github-actions-au-quotidien/`, la section `// projets liés` porte le **titre** du projet → clic → `/projets/gha-svu/` → clic sur l'article lié → retour sur l'article. Aller-retour complet, **aucun `undefined`** dans le DOM. Nav vérifiée : `Blog` et `Projets` sont des `<a>` ; `Prompts`, `Skills`, `À propos` restent des `<span>` non cliquables — aucun lien mort introduit.
  - **375px** en dark : `scrollWidth == innerWidth == 375`, aucun élément débordant, en-tête sur une ligne. Les 4 articles sans `relatedProjects` ne rendent **aucune** section.
  - **Revue de tâche : spec ✅ / qualité Approved — 0 Critical, 0 Important, 0 Minor.**
  - **R5 → Done** (les deux sens rendus, liens cliquables, aucun `undefined`). **R7 → Done** : les 4 tâches livrant du gabarit ont chacune été contrôlées à 375px, en dark **et** en light, avec la liste des éléments débordants **vide** à chaque fois.
- 2026-07-31 — **6 critères sur 7 `Done`. Reste R6**, dont la clause « créer un projet via `/admin` produit un fichier conforme au schéma » exige **un geste que seul l'utilisateur peut poser** (T-C2). **Phase Z NON lançable** : elle exige 0 déviation `pending-user`, or il y en a **6** (D01→D06).
- 2026-07-31 — **REVUE FINALE DE BRANCHE** (opus, 20 fichiers de code, sondes et injections réelles) : **aucun Critical**, merge recommandé sous réserve de **2 Important** — tous deux invisibles pour une revue par tâche, car nés de la **composition** de plusieurs tâches.
  - **I1** — aucune collection ne pose `delete: false` ; le défaut Sveltia/Decap est `true`. Or le CMS **ne nettoie pas les rétro-références**. Reproduit : supprimer le projet `gha-svu` depuis `/admin` laisse `relatedProjects: [gha-svu]` sur l'article → `astro build` s'interrompt → **0 page produite**, le site entier cesse d'être déployé. **Troisième occurrence** du mode « le CMS accepte, le build casse après le commit » sur ce projet.
  - **I2** — le garde-fou anti-dérive CMS↔Zod n'est monté que d'un côté : `projects.relatedPosts` est vérifié sur son `value_field`, `blog.relatedProjects` ne l'est que sur son nom. Passer `{{slug}}` à `{{title}}` laissait 40/40 verts, `astro check` vert et `astro build` vert — puis le **premier article enregistré depuis `/admin`** cassait le build.
  - **Tri des Minor déférés par la revue finale** : à corriger → le JSDoc faux de `sortProjects`. Peuvent rester, avec motif établi → `ProjectStatus` retapé (la dérive est **rattrapée** par `astro check`) · la `border` de la puce (nécessaire : `archivé` est neutre sur neutre) · le `try/catch` de `data-stack` (prouvé **inatteignable** : la valeur ne vaut jamais que `JSON.stringify` d'un `z.array(z.string())`) · les écarts de marge (anatomies différentes) · les liens externes — **la prémisse du constat était fausse**, `Hero.astro` n'a pas d'annonce lecteur d'écran non plus.
  - **Chemins non exercés, vérifiés** : `cover` (un nom nu se résout et s'optimise), `archivé` (les 3 puces sont bien dans le CSS compilé, `archivé` en NFC partout), bundle client (aucune fuite). **`tags` est déclaré au schéma, exposé au CMS et rendu nulle part** — mort, exactement comme le `tags` du blog depuis le Plan 1. Constat reporté, hors périmètre du Plan 3 (les pages de tags sont le Plan 5).
- 2026-07-31 — **Déviations D07 et D08 loggées avant exécution** ; **GATE DE DÉCISION UTILISATEUR** : « **Je les approuve toutes** » → **D01→D07 `approved`** ; sur I1, « **On suit ta recommandation** » → délégation explicite du choix parmi les trois options présentées. **Le log de déviations ne contient plus aucune entrée `pending-user`.**
- 2026-07-31 — **Vague de correctifs de revue finale** → commits `dd95656` et `2dfc374`.
  - **I2 fait** : test miroir sur `blog.relatedProjects` (`widget`, `collection`, `multiple`, `value_field`). **Minor fait** : le JSDoc de `sortProjects` décrit désormais le comportement réel de `localeCompare('fr')`, re-vérifié en Node par le relecteur (casse et accents départagent, ils ne sont pas ignorés).
  - **I1 fait (D08)** : `delete: false` sur **les deux** collections + assertion de test + section README « Supprimer une entrée proprement », qui couvre explicitement **les deux sens** de rétro-référence et recommande un `grep` sur le slug puis un build local avant de pousser. **Fait établi par le contrôleur sur le bundle épinglé**, pas sur la doc : `let r = t.delete ?? !0` puis `canDelete: r`, consommé réactivement par l'UI — `delete: false` désactive donc réellement la suppression. L'implémenteur avait honnêtement signalé qu'il ne pouvait pas le vérifier ; le contrôleur l'a fait.
  - **Re-revue ciblée : I1 ADDRESSED, I2 ADDRESSED, aucun nouveau défaut.** Le relecteur a prouvé que les tests ne sont pas des coquilles en **mutant les 4 champs un par un** puis en restaurant : chaque mutation fait échouer l'assertion attendue. Version du CDN, `create`, médias entry-relative, `choose_url` et `omit_empty_optional_fields` vérifiés **intacts** sur les deux collections.
  - État final vérifié : `vitest` **42/42**, `astro check` **0 error / 0 warning / 33 hints**, `astro build` **11 pages**, arbre propre.
- 2026-07-31 — **ÉTAT : 6 critères sur 7 `Done`, 0 déviation `pending-user`.** Le seul reste est **R6**, dont la clause « créer un projet via `/admin` produit un fichier conforme au schéma » exige **un geste que seul l'utilisateur peut poser** (T-C2 : ni le PAT ni le sélecteur de dossier natif ne sont accessibles à un agent — établi au Plan 2). **Phase Z non lançable tant que R6 n'est pas couvert.**
- 2026-08-02 — **T-C2 exécutée par l'utilisateur. R6 → Done. 7 critères sur 7 `Done`.**
  - **Fichier produit par le CMS** : `src/content/projects/test/index.md` — structure de page bundle conforme. `tags`, `cover`, `coverAlt` **absents** (pas vides) : `omit_empty_optional_fields` est prouvé **en conditions réelles** sur la collection `projects`, comme il l'avait été sur `blog` au Plan 2. `featured: false` conservé — `false` n'est pas traité comme vide. `repoUrl`/`demoUrl` acceptés par `z.string().url()` après passage du `pattern` durci de **D03**.
  - **Le widget `relation` est enfin prouvé par un clic humain**, et non plus seulement par lecture du bundle. Le CMS a écrit `relatedPosts: [k9s-kubernetes-terminal-ui, bienvenue-dans-mon-foutoir]` — **exactement les noms de dossiers** sous `src/content/blog/`, donc les `id` d'entrée Astro. La déduction faite en T-C1 sur `value_field: '{{slug}}'` est confirmée. C'était **la dernière hypothèse non vérifiée du plan** : si le widget avait écrit le titre, toute liaison faite depuis `/admin` aurait produit une référence morte et cassé le déploiement public.
  - **Chaîne complète vérifiée** : CMS → fichier → schéma Zod → `astro build` (12 pages) → carte sur `/projets` (`data-status="wip"`, `data-stack="[\"rust\"]"`) → fiche avec ses deux boutons et ses **deux** articles liés rendus avec leur titre, **0 `undefined`**. L'ordre de la grille s'est vérifié au passage : `site-bencat` (featured) puis `test` (2026-08) puis `gha-svu` (2025-05) — conforme à `sortProjects`.
  - **D08 vérifiée visuellement par l'utilisateur** : le bouton **Delete** est **grisé** dans le CMS. C'était le seul point que l'implémenteur n'avait pas pu établir autrement qu'en lisant le bundle (`canDelete = t.delete ?? !0`) ; il est maintenant observé à l'écran.
  - **Nettoyage fait en suivant le README** (la suppression CMS étant désactivée) : recherche des rétro-références citant `test` → **aucune** ; `rm -rf src/content/projects/test` ; re-build. État final : `vitest` **42/42**, `astro check` **0 error / 0 warning / 33 hints**, `astro build` **11 pages**, arbre **propre**.
- 2026-08-02 — **7/7 critères `Done`, 0 déviation `pending-user`, toutes les tâches livrées (A1, A2, B1, B2, B3, B4, C1, C2).** Phase Z (`/anti-drift-planning:verify 3`) peut être lancée.
- 2026-08-02 — **PHASE Z — `/anti-drift-planning:verify 3` → VERDICT : ✅ PASS.**
  - **Lint mécanique (Lock 5)** : **0 violation sur 13 contrôles**, exit 0. Premier passage bloqué à **6 violations** dont **aucune n'était imputable au Plan 3** : la table §4 de la méthodologie avait 3 colonnes là où le linter en exige 4, rendant tout le registre illisible (« *roster plans: none* »). Vérifié : les plans 1 et 2 sortaient **exactement les mêmes 6 violations** — un contrôle plus récent que le fichier qu'il audite. Réparé sous **déviation D09** (approuvée). Le registre réparé a ensuite démasqué une erreur du contrôleur : les statuts du ledger étaient écrits **en gras**, que le linter refuse comme littéral — corrigé, à raison.
  - **Couverture spec** : **7/7 critères `Done`** (R1–R7), audités sur les lignes de la **spec**. Aucun `Deferred`, aucun `Cut`, aucune ligne `Pending` ou `In progress`.
  - **Déviations** : **9 entrées, 9 `approved`, 0 `pending-user`** — le seul `pending-user` du fichier est dans le bloc template. Aucun statut inventé : le garde-fou de vocabulaire du linter le confirme mécaniquement. Trois de ces déviations (**D04, D05, D07**) sont des **corrections d'erreurs factuelles du plan d'implémentation**, remontées par des implémenteurs qui ont refusé de les contourner.
  - **Tests (passage frais, pendant l'audit)** : `vitest` **42/42** sur 4 fichiers · `astro check` **0 error / 0 warning / 33 hints** · `astro build` **11 pages** · lint JS/CSS : aucun linter configuré dans ce projet (constat, pas un contournement) · arbre **propre**.
  - **Walkthrough user story (spec §1)** : **5/5 ✓**, confirmés par l'utilisateur. Preuves : grille à 2 cartes avec les 4 champs ; matrice de filtre complète mesurée dans le navigateur (statut, techno, combinaison **ET**, état vide sur `archivé`, retour à l'état initial) ; fiches rendues avec corps, liens repo/démo et articles liés ; **aller-retour article ↔ projet bouclé par clics réels** ; création d'un projet via `/admin` par l'utilisateur, avec deux articles liés écrits par le widget `relation` sous forme d'**id** — la dernière hypothèse non vérifiée du plan.
  - **Smoke visuel** : ✓ — `/projets` en dark et en light, à 1280px et à **375px** (`scrollWidth == innerWidth`, liste des éléments débordants **vide**) ; les deux fiches ; la page article avec sa section « projets liés » ; le CMS avec la collection Projets et le bouton **Delete grisé** (preuve visuelle de D08).
  - **Z1 → Done. Plan 3 vérifié.** Ship (script de release + tag `milestone-plan-3`) = **action utilisateur**, distincte de cet audit.
- 2026-07-31 — **Constat de pré-flight, hors périmètre du Plan 3** : le tag `milestone-plan-2` et la version `v0.2.0` **n'existent pas** (`git tag -l` → `milestone-plan-1`, `plan-2-b2-evidence`, `v0.1.0`). La Phase Z du Plan 2 a bien rendu un verdict PASS (`9793f6b`), mais le ship — script de release + tag — était noté comme une action utilisateur distincte de l'audit et n'a pas été faite. La spec du Plan 3 dit « off `main` @ `milestone-plan-2` » : la branche part donc du commit **que ce tag devrait désigner**. Aucun impact sur l'exécution du Plan 3 ; à trancher par l'utilisateur (poser le tag rétroactivement ou l'assumer manquant).
