# Run report — astro-bencatdev · 2026-09-24 (plans 9–11)

## At a glance
3 plans shipped · 0 parked · 7 decisions to re-read · 1 open escalation (publication `v1.4.0`)

Second `anti-drift-auto` run on this repo. It closes wave 2 of the redesign: the contract's plan 9
`accueil-et-finition` was split into three slices (home, secondary pages, site-wide finish).
15 tasks + 2 fix tasks, 4 verification attempts (plan 11 needed a second one), 39 decisions logged
(D24–D62), 2 questions asked at cadrage, 0 questions during the run. Integration branch
`claude/anti-drift-auto-start-532f11`, tip `c816bf6`, 31 commits ahead of `main`. Tests 168 → 222.

## Decisions to re-read (⚑)
| D | Plan | Decision | Why | To reverse |
|---|---|---|---|---|
| D27 | 9 | The home hero goes; the `~/ whoami` identity lives on `/a-propos` | The prototype's home has no hero; it puts whoami on the about page | Re-add a hero above row 1 in `src/pages/index.astro` (its text is now in `src/lib/about.ts` / `src/components/about/Identity.astro`) |
| D42 · D48 | 10 | `/a-propos` built only from existing author text and derivations: `Qui je suis` + `Pourquoi ce site` cards (no `On parle ?` — no email, no LinkedIn); terminal lines derived (`terrain` = 3 most used tags, `écrit` = published counts, `stack` = project stacks) | D26: no invented sentence, no prototype demo value | Edit `ABOUT_FACTS` / `whoamiLines` in `src/lib/about.ts`; add a contact card when you have a channel to publish |
| D43 · D50 | 10 | Stack logos: 6 Simple Icons paths (CC0) inlined in `currentColor`, nominative use of third-party trademarks; GitHub Pages shown as text (its mark is an unreadable wordmark at 14 px) | Self-hosted, no CDN (D26) | Remove entries from `src/lib/stackLogos.ts` (text-only pills) |
| D52 | 11 | Code blocks use a token-built Shiki theme (keywords rose, strings blue, constants amber, functions violet, parameters green, keys accent, comments `muted`) instead of github-light/dark | The GitHub dark comments were 4.07:1 on `code` (AA fails), and the goal forbids colours outside the contract | Change the role → token map in `src/lib/syntaxTheme.mjs` (tests keep every colour ≥ 4.5:1) |
| D54 | 11 | "Every rendered pair" measures text at rest over composited backgrounds (hatch, hover, emoji, native UI out); "no colour outside the contract" = the 37 colour tokens at any alpha, ±2 per channel | Makes the roster goal measurable and explicit | Tighten `scripts/audit-rendered.js` and re-run it |
| D60 → D61 | 11 | Focus ring: 2 px `accent` everywhere; the tabs and their panel keep their plan-6/8 `ink` ring | The verifier refused widening the `ink` exception to the header; the header moved to `accent` (F1) | Switch `focus-visible:outline-ink` in `src/components/DetailTabs.astro` to `outline-accent` |

## Shipped
| Plan | Goal (R0) | Where to see it | Milestone | Evidence |
|---|---|---|---|---|
| 9 · accueil | The home follows the prototype: featured card + `Derniers articles` (3 posts, thumbnails), panels Projets · Prompts · Skills, AI-transparency banner; every link resolves; no overflow at 375 px | `/` | `milestone-plan-9` on merge `6d4d8a7` | 15/15 proven — `docs/auto/plans/9-accueil.md#evidence` |
| 10 · pages-secondaires | `/a-propos` in the prototype's structure from the repo's own facts; `/transparence-ia`, `/tags`, `/tags/<tag>`, `/404`, search on the contract patterns; every tag chip toned by a stable hash; AI levels on contract tones, distinct in both themes | `/a-propos/`, `/tags/`, `/transparence-ia/`, any tag chip | `milestone-plan-10` on merge `05ceeff` | 18/18 proven — `docs/auto/plans/10-pages-secondaires.md#evidence` |
| 11 · finition | Every route, both themes: no overflow at 375 px, every rendered text pair AA, no surface-level jump, no colour outside the contract, list counts announced (`aria-live`) | Everywhere — e.g. `/projets/` (lists framed in one `.card`), any article code block, Tab from the top of any page | `milestone-plan-11` on merge `c816bf6` | 19/19 proven (attempt 2) — `docs/auto/plans/11-finition.md#evidence` |

What changed for a visitor:
- **Home**: no hero any more; featured post card beside the three latest posts, three section panels, AI banner linking to `/transparence-ia/`.
- **About**: `~/ whoami` identity, a terminal `whoami --long` block, the author's two texts, the AI rule panel with the three markers, and a toolbox of the project stacks with logos.
- **Tags**: each tag has one of the 5 contract tones, the same everywhere (`devops` green, `claude-code` amber…).
- **AI transparency**: 100% humain green, co-créé amber, IA relue blue. In the light theme, the « 100 % humain » banner is finally visible: it was at about 1.02:1 against the page since plan 6.
- **Lists**: entries sit in one framed card (the surface-hierarchy defect deferred since plan 7 is gone). The result count is announced to screen readers, and the empty state is centred.
- **Everywhere**: a green focus ring, code blocks recoloured on the contract, a pale search backdrop in light, and no text below AA.

Defects caught by rendering, not by tests (the plans 6–8 lesson held again):
- **Home (plan 9, `4552221`)**: the featured meta read `DevOps ·28 octobre`, because Astro drops a newline-only gap between text and an element.
- **Toolbox (plan 10, D50)**: the GitHub Pages logo was an unreadable smudge at 14 px.
- **Plan 11 (F1, F2)**: the header kept an `ink` focus ring, and the empty state sat off-centre because a zero-height grid still took a flex gap.

## Parked
| Item | Why | Suggested next step |
|---|---|---|
| (none) | — | — |

## Open escalations
| E | Question | Recommendation |
|---|---|---|
| E1 | Publish wave 2 (plans 9–11) as `v1.4.0` | A — publish: `git -C /Users/bencat/workspace/perso/astro-bencatdev merge --ff-only claude/anti-drift-auto-start-532f11 && git -C /Users/bencat/workspace/perso/astro-bencatdev tag -a v1.4.0 -m "Vague 2 terminée — accueil, pages secondaires, finition" && git -C /Users/bencat/workspace/perso/astro-bencatdev push origin main v1.4.0 milestone-plan-9 milestone-plan-10 milestone-plan-11` (a push to `main` triggers GitHub Pages), then a production check by the orchestrator |

## Smoke checklist — only what no agent could reach, about two minutes
Every criterion was measured locally by an agent. The production check after publication is done
by the orchestrator in the built-in browser. What is left for you is taste, not correctness:
- [ ] `/a-propos/` → you recognise yourself in the page (D42, D48); the toolbox logos are acceptable to you (D43)
- [ ] Any article with code (e.g. `/blog/docker-kubernetes-devops/`) → you like the new code colours in both themes (D52)
- [ ] `/` → you are fine without the hero (D27)

## Follow-ups (outside the criteria, none blocking)
- **Focus ring fade-in.** It fades in over ~150 ms, because `transition-colors` also animates `outline-color`. Dropping `outline-color` from those transitions would make it instant.
- **Search dialog in light.** It stands out from its 85 % `bg` wash only by its border and shadow.
- **Narrow cards at 375 px.** The list frame's nested padding leaves ~279 px for card content.
- **Native clear button.** The search field's clear button is WebKit blue. That is browser UI, unchanged since plan 5.
- **Hidden derived thumbnail on the home.** A post without a cover would put a derived thumbnail directly on `surface`. It does not happen today; `check-finition.mjs` fails the day it does.
- **Stale methodology index.** `docs/anti-drift/` (frozen) still ends at plan 7. The source of truth for plans 8–11 is `docs/auto/`.
- **Custom domain.** Still deferred since plan 1.

## All decisions
See `docs/auto/decisions.md` (D24–D62 for this run).
