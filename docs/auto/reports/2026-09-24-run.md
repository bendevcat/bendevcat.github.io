# Run report — astro-bencatdev · 2026-09-24

## At a glance
1 plan shipped · 0 parked · 4 decisions to re-read · 1 open escalation

First `anti-drift-auto` run on this repo, adopting plan 8 of the `anti-drift-planning` roster
(plans 1–7 were run with the old plugin). 4 tasks, 1 fix round, 1 verification attempt,
22 decisions logged, 1 question asked at cadrage, 0 questions during the run.

## Decisions to re-read (⚑)
| D | Plan | Decision | Why | To reverse |
|---|---|---|---|---|
| D2 | 8 | Skill body goes in a first tab labelled "Aperçu" (skills: Aperçu · Infos) | The prototype's Déclencheurs / Versions have no data; "Aperçu" is the prototype's label for a body on project pages | Rename the label in `skillTabs` (`src/lib/detailTabs.ts`) and its test |
| D4 | 8 | The literal `No content` body of `macos-clone` and `superpowers` counts as content and fills a tab | Treating it as blank leaves one tab, so no tab row, and R0 ("every existing entry") fails | Replace the placeholder text in the content, or treat it as blank in `isBlank` and accept a tab-less page |
| D7 | 8 | Prompt and skill header metadata chips (format, tool, model, tags, related items / type, name, version…) move into the Infos panel | Infos is the prototype's home for that metadata; showing it twice makes Infos redundant | Put the chips back in the header of `src/pages/prompts/[...slug].astro` / `src/pages/skills/[...slug].astro` |
| D19 | 8 | No-JS-only headings (`// aperçu`, `// stack`…) precede each panel, hidden when JS runs | Labels the stacked panels without JS and keeps the page's existing words (R11) | Drop `data-tab-fallback-heading` headings from `DetailTabs.astro` |

## Shipped
| Plan | Goal (R0) | Where to see it | Milestone | Evidence |
|---|---|---|---|---|
| 8 · details-a-onglets | The three detail pages (project, prompt, skill) have the tabbed-detail pattern of contract §5.2 — active/inactive looks in both themes, a tab only where content feeds it, no schema or CMS change | Home › Projets / Prompts / Skills › any card › tab row (e.g. `/projets/gha-svu`: Aperçu · Stack · Articles liés) | `milestone-plan-8` on merge `881fde8` — **not published** (E1) | 16/16 criteria proven — `docs/auto/plans/8-details-a-onglets.md#evidence` |

What changed for a visitor:
- Projects: Aperçu (cover + body) · Stack · Articles liés (gha-svu only — site-bencat's only related post is a draft).
- Prompts: the prompt block with **Copier** stays above the tabs; Pourquoi (body) · Infos (format, tool, model, tags, related skills).
- Skills: Aperçu (body) · Infos (type, name, version, tags, install command with **Copier**, related prompts).
- Keyboard: arrows (wrapping), Home/End; Tab moves into the panel. Without JavaScript every panel shows under its own heading.
- Omitted for lack of data (cadrage decision D1, "pattern first"): prompt Variables and Sortie, skill Déclencheurs and Versions, stack roles, numbered code block, skill file tree.

One real defect was caught by rendering, not by tests: without JavaScript only the first panel
showed, because Tailwind v4's preflight `[hidden]{display:none!important}` sits in `@layer base`,
and a layered `!important` beats an unlayered one. Fixed in `781b7d1` (D22); the check script now
guards the layer placement.

## Parked
| Item | Why | Suggested next step |
|---|---|---|
| (none) | — | — |

## Open escalations
| E | Question | Recommendation |
|---|---|---|
| E1 | Publish plan 8: fast-forward `main` to `claude/plan-8-anti-drift-auto-abb7ce` (`881fde8`), annotated tag `v1.3.0`, push `main` + `v1.3.0` + `milestone-plan-8` (triggers GitHub Pages) | A — publish: `git -C /Users/bencat/workspace/perso/astro-bencatdev merge --ff-only claude/plan-8-anti-drift-auto-abb7ce && git -C /Users/bencat/workspace/perso/astro-bencatdev tag -a v1.3.0 -m "Plan 8 — details-a-onglets" && git -C /Users/bencat/workspace/perso/astro-bencatdev push origin main v1.3.0 milestone-plan-8` |

## Smoke checklist — only what no agent could reach, about two minutes
- [ ] After publication, the GitHub Actions "Deploy to GitHub Pages" run for `881fde8` → `success`
- [ ] `https://bendevcat.github.io/projets/gha-svu/` → three tabs; clicking Stack shows only the stack chips
- [ ] `https://bendevcat.github.io/skills/superpowers/` → Infos › Copier → label `Copié !`, clipboard holds the install command
- [ ] Toggle the theme on any detail page → the active tab keeps a visible card background and border

## Follow-ups (outside the criteria, none blocking)
- The no-JS fallback headings sit inside `<article data-pagefind-body>`, so Pagefind indexes `// aperçu`, `// infos`… on every detail page — `data-pagefind-ignore` on them would remove that noise.
- The unlayered `[data-detail-tabs] [role="tabpanel"][hidden]{display:none!important}` duplicates the preflight rule.
- The CSS-layer check in `scripts/check-detail-tabs.mjs` parses minified output with a regex.
- A comment in `src/pages/skills/[...slug].astro` cites `global.css` line numbers that predate this plan.
- `docs/anti-drift/` still ends at plan 7; `/anti-drift-planning:status` does not know plan 8 — a pointer to `docs/auto/` in the methodology would keep one source of truth.
- Plan 9 (accueil-et-finition) is not in this run; its inherited items are listed in `docs/auto/brief.md` › Context.

## All decisions
See `docs/auto/decisions.md`.
