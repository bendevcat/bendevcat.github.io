# Run report — astro-bencatdev · wave 4 « board Sveltia » · 2026-09-25

## At a glance
5 plans shipped (P19–P23) · 0 parked plan (1 parked criterion: P21 R16's Save clause, D147, accepted by the user in E2) · 21 decisions to re-read · 1 open escalation (publication `v1.6.0`)

Integration branch `claude/sveltia-cms-integration-e3d469`, tip after plan 23 = `1f54020`; tags `milestone-plan-19` … `milestone-plan-23` (local). 399 → 654 tests, all green; `npm run check` 0 errors. Escalations answered during the run: E1 (A — content in Sveltia's canonical markdown, D145), E2 (A — integrate P21 with R16 parked, frontmatter aligned in P22, D148).

## Decisions to re-read (⚑)
| D | Plan | Decision | Why | To reverse |
|---|---|---|---|---|
| D124 | 19 | `/admin/?test-repo` (dev only) loads a demo board seeded from `src/content` into the browser's OPFS | agents cannot type the PAT nor use the directory picker | remove `src/admin/dev/`, the DEV branch of `cms.ts` |
| D127 | 19 | `delete: true` on the 4 collections — Sveltia 0.221 removes the deleted slug from other entries' relation fields in the same commit (proven by walkthrough + build) | D08's cause no longer holds | `delete: false` ×4 |
| D128 | 19 | CDN script + SRI dropped; integrity rests on `package-lock.json` | Architecture B (npm) | back to the CDN page |
| D131 | 19 | wave-3 content tests keep generic rules, drop per-entry snapshots | a CMS delete/edit would block the CI deploy | restore the pins |
| D133 | 19 | list of what content tests still guard (D102 licence rule now author-enforced) | D131 | idem |
| D134 | 20 | Brouillons/Publiés filters not on projects (no `draft` field); a draft's « view on site » link points to its future URL (404 until published) | brief's "everywhere" impossible literally | add `draft` to projects |
| D136 | 20 | P20's R19 excluded a preview-image race (fixed in P21); R5 judged on drawn glyphs | instrument defeated by Sveltia's CDN font face | — |
| D139 | 21 | "saving an unchanged entry rewrites byte for byte" read as three checks | Sveltia re-serialises frontmatter on any save | — |
| D140 | 21 | preview choices (centre column, light theme, omitted parts); **iframe keeps Sveltia's `allow-scripts` + `allow-same-origin` sandbox** | not changeable without patching Sveltia | — |
| D141 | 21 | preview images get a `src` only for `blob:`/`data:`/`http(s):` | no 404, no untrusted address | — |
| D142 | 21 | `code` widget on prompt/snippet/excerpt; **Sveltia's code highlighter fetches Shiki from unpkg.com in `/admin`** | Sveltia behaviour | self-host via `setCodeHighlighterLoaders` |
| D143 | 21 | preview body HTML sanitised (payloads proven inert in the real iframe) | content from a PR could run code in `/admin` | — |
| D144 | 21 | DOMPurify 3.4.16 config (`SANITIZE_DOM` off so heading ids survive; `name`, `<style>`, `<form>` removed) | D143 | — |
| D146 | 21 | decouper (guide shown verbatim) and the VPN list canonicalised too; `~1597` → `~1598 tk` | otherwise a CMS save publishes `\*\*` | — |
| D147 | 21 | R16's Save clause parked: Sveltia turns Save on when a `tags`/`stack` list mounts | Sveltia behaviour; P22 then made unedited saves byte-identical | — |
| D149 | 22 | rule B reads the last committed file (public GitHub contents API, **no token**, `no-store`); unknown → dates untouched | Sveltia's `preSave` has no previous state | — |
| D150 | 22 | Duplicate keeps the original's `draft`; commit prefix renders the singular label (`content(Article)`); ✏️ marker `localStorage['bencat:author']` is a **convenience, not access control**; bookmarklets carry the selection in the URL hash | see plan 22 | — |
| D152 | 23 | block syntax and French names (`:::note/astuce/attention/danger`, `:::terminal[…]`, `:::carte{ref}`, `:::video[…]{youtube\|asciinema}`), callout tones | public syntax — expensive once content uses it | rename before first use |
| D153 | 23 | video facade: nothing from YouTube/asciinema before the click, youtube-nocookie, plain link without JS | privacy | — |
| D155 | 23 | @sveltia/ui editor files (MIT) vendored for the test replica; `lexical` pinned 0.51.0 | test Sveltia's real serialiser | re-copy on each Sveltia bump |
| D158 | 23 | Terminal code via a `text` field (Sveltia's code editor destroyed code containing ```) — supersedes D156 | data loss found by verification | — |

## Shipped
| Plan | Goal (R0) | Where to see it | Milestone | Evidence |
|---|---|---|---|---|
| 19 socle-admin | `/admin/` = Astro page on Sveltia 0.221 (npm, exact pin), dev and prod; site CSS in the preview; only `---` rules; delete re-enabled with proven back-reference cleanup | `npx astro dev` → `/admin/` ; `/admin/?test-repo` | `milestone-plan-19` | `docs/auto/plans/19-socle-admin.md` § Evidence (19/19, attempt 2) |
| 20 navigation | title `benCat · Studio`, logo, icons, summaries 📝/⭐, default sorts, filters, groups, « Afficher sur le site en ligne » | `/admin/?test-repo` → any collection | `milestone-plan-20` | `20-navigation.md` (21/21, attempt 2) |
| 21 previews | each entry previews as its page's centre column, same text as the site (13/13), site pipeline + Shiki `syntaxTheme`, code widget, sanitised | `/admin/?test-repo` → MacOS Clone, k9s | `milestone-plan-21` | `21-previews.md` (20/21 + R16 parked, D147/D148) |
| 22 creation-rapide | `pubDate` now, `draft: true` on creation, rule-B dates, WebP ≤ 1600 px, `content(…)` commits, Duplicate, `/admin/raccourcis` bookmarklets, author-only ✏️; frontmatter canonical (unedited save byte-identical 13/13) | `/admin/?test-repo` → Nouveau ; `/admin/raccourcis` ; ✏️ after one `/admin` visit | `milestone-plan-22` | `22-creation-rapide.md` (25/25, attempt 2) |
| 23 blocs-editeur | four blocks (Encadré, Terminal, Carte, Vidéo) from the editor's Insérer menu, `:::` syntax, rendered in the preview and on the site, round-trip identical | `/admin/?test-repo` → an article → Insérer | `milestone-plan-23` | `23-blocs-editeur.md` (21/21 + T4b, attempt 2) |

Site impact for visitors: 6 pages differ from `v1.5.0` by content syntax only (whitespace around bold on 3 pages; decouper page and `/prompts` card `~1598 tk`; the VPN article's nested list tightened); every detail page carries a hidden ✏️ link (shown only to the author); no content uses the new blocks yet.

## Parked
| Item | Why | Suggested next step |
|---|---|---|
| P21 R16 — "Save stays disabled on an unedited entry" | Sveltia 0.221 marks an entry modified when a non-empty `tags`/`stack` list widget mounts (also at base) | none needed: P22 made an unedited save byte-identical (R20 13/13); report upstream to Sveltia if the enabled button bothers you |

Known limits kept (documented in README): the fence-toggle case (odd ```-lines in terminal code followed by an indented list or `>` quote) is caught by the test guard at deploy, not in the CMS; inserting a block at the start of a heading turns it into a paragraph (Sveltia); `/admin/raccourcis` is light-theme only; the card picker's entry list refreshes at the next deploy / dev restart.

## Open escalations
| E | Question | Recommendation |
|---|---|---|
| E3 | Publish wave 4 as `v1.6.0` (fast-forward `main`, tag, push `main` + `v1.6.0` + `milestone-plan-19…23`, production check)? | A — 5/5 plans shipped, 654 tests green, site pages unchanged for visitors except the 6 syntax-only pages |

## Smoke checklist — only what no agent could reach, about two minutes
- [ ] https://bendevcat.github.io/admin/ → « Se connecter avec un jeton d'accès » → paste your PAT → board shows Articles 6 · Projets 2 · Prompts 3 · Skills 2
- [ ] Edit a published article's body → Save → the commit on `main` reads `content(Article): update "<slug>"` and carries `updatedDate` at `:00` seconds
- [ ] Upload a PNG as a draft's cover → Save → a `.webp` (≤ 1600 px) lands beside `index.md` and the deploy passes
- [ ] Duplicate an article → Save → the new folder and its cover copy exist in the repo (then delete it from the CMS)
- [ ] While signed in, open a detail page → click ✏️ Éditer → the entry opens directly
- [ ] Drag 💡 from `/admin/raccourcis` to your bookmarks bar → select text on any page → click it → a pre-filled draft article opens

## All decisions
See `docs/auto/decisions.md` (D124–D158).

## Production check — v1.6.0 (E3 answered A, D159)
Published: `main` fast-forwarded to `332dcdd`, annotated tag `v1.6.0`, pushed with `milestone-plan-19` … `milestone-plan-23`. GitHub Actions run `36180096057` « Deploy to GitHub Pages »: test ✓ · build ✓ · deploy ✓.

Checked in the built-in browser on https://bendevcat.github.io:
- `/admin/` → title `benCat · Studio`, sign-in screen (« Se connecter avec GitHub », « Se connecter avec un jeton d'accès »), no test-repo button; the author marker `bencat:author` is set on load.
- 14 routes answer 200: `/`, `/blog/`, `/blog/k9s-kubernetes-terminal-ui/`, `/blog/meilleurs-vpn-2025/`, `/projets/gha-svu/`, `/prompts/`, `/prompts/macos-clone/`, `/prompts/decouper-un-projet-en-plans-anti-drift/`, `/skills/superpowers/`, `/admin/raccourcis/`, `/admin/config.yml`, `/admin/logo.svg`, `/tags/`, `/a-propos/`.
- k9s renders its rules as `<hr>` (no « - - » bullet); decouper page and `/prompts` card show `~1598 tk`.
- ✏️ Éditer on k9s: served `hidden`; shown (`display: block`, in the viewport, bottom-right) with the marker; href `/admin/#/collections/blog/entries/k9s-kubernetes-terminal-ui/index`.

The six smoke points above (PAT sign-in, real commit text, WebP in the repo, Duplicate, ✏️ while signed in, bookmarklet) remain yours.
