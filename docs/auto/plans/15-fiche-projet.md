# Plan 15 — fiche-projet

Brief: `docs/auto/brief.md` · Branch: `auto/plan-15-fiche-projet` · Base: `6c442c047476512ddbdb05ce8efa13c82731489b`

## Goal
From `/projets`, the user opens any project and sees the prototype's project page (inventory §4): a main surface card (banner, `← tous les projets`, title, description, **underline** tabs Aperçu · Stack (n) · Articles liés (n) — a tab without data is omitted) beside a 280 px sidebar (statut, depuis, licence, full-width `code source ↗`; Articles liés); Stack shows one tile per tech with its logo (text when none) and its role when sourced; Aperçu shows a dark line-numbered code window with Copier when the project has a code snippet

## Reachability
From `http://localhost:4321/`: header nav `Projets` › `/projets/` › the hero title or a grid card title › `/projets/<id>/` (also: the home's Projets panel, an article's `Projets liés` card, search results). The path exists; this plan changes what `src/pages/projets/[...slug].astro` renders. From the page: `← tous les projets` › `/projets/`; an Articles liés tile or sidebar link › `/blog/<id>/`; `code source ↗` / `démo ↗` › `repoUrl` / `demoUrl`.

### Built facts (measured at base, `npm run build`)
Two projects, neither with a `cover`, `license` or code fence. `gha-svu`: actif, startDate 2025-05-05, stack `GitHub Actions, Bash, Go, SVU`, 1 published related post (`comment-jutilise-github-actions-au-quotidien`), repoUrl only. `site-bencat`: wip, 2026-07-30, stack `Astro, Tailwind CSS, TypeScript, Sveltia CMS, GitHub Pages`, its only related post `bienvenue-dans-mon-foutoir` is `draft: true` (0 published), repoUrl + demoUrl. `STACK_LOGOS` covers Astro, Tailwind CSS, TypeScript, GitHub Actions, Bash, Go — not Sveltia CMS, GitHub Pages, SVU. `check-detail-tabs` → 7 rows (`projets/gha-svu: Aperçu | Stack | Articles liés`, `projets/site-bencat: Aperçu | Stack`, prompts and skills `Pourquoi | Infos` / `Aperçu | Infos`); `check-finition` → `v2: 0 card-level on bg · 0 rail off card level · 11 rail column · 3 derived thumbnails`; the other six instruments print their plan-14 lines; `npx vitest run` → 312 passed.

### Sources (Content rule, D65)
- **Licence**: no LICENSE in this repo nor in any local `bencatlab/gha-svu` clone → `license` joins the schema, stays empty; the licence row is omitted on both pages.
- **Snippets** (verbatim whole files, filename chip = path in the project's own repo): `site-bencat` ← this repo's `.github/workflows/deploy.yml` (35 lines, read only); `gha-svu` ← `~/workspace/tools/gha-svu` (tags v0.0.1–v0.0.3, matching the body), `git show cbf8c36:.github/workflows/check-pr.yml` (30 lines — a working use of the action; the README example is not used: it pins a nonexistent `@v0.1.0` and reads `steps.svu` without `id: svu`).
- **Stack roles**: verbatim fragments of the project's own title, description or body (whitespace collapsed, `*` stripped): `site-bencat` Astro `statique`, Tailwind CSS `thème`, Sveltia CMS `écrire sans toucher au code`, GitHub Pages `déploiement`; `gha-svu` GitHub Actions `action composite`, SVU `prochain numéro de version`. TypeScript, Bash, Go: no role (the body does not state one).
- Both `src/content/projects/*/index.md` files get a decision entry `⚑ à relire` naming these sources.

### Design rules (prototype 433–522 via inventory §4; authoring choices listed as decision candidates)
- **Page**: `<article data-pagefind-body>` holding a grid, `pt-10`; from 1024 px `minmax(0,1fr) 280px`, gap 26; below 1024 one column, main card then sidebar (DOM order).
- **Main card** `.card overflow-hidden` (radius 20, `surface`): banner = `Thumbnail` new size `banner` (150 px, full width, no own radius; real `cover` with `coverAlt ?? title` when present, else the bare derived hatch with `data-rail` + `data-thumb-derived`, like `tile`/`hero`). Header padding 28/32/0 (20 px sides below 640): `← tous les projets` `<a href="/projets/">` mono 11 `muted` (hover `ink`, `data-pagefind-ignore`); h1 32 px / 700 `ink`, `text-pretty`; description 16 px `muted`, `max-w-[600px]`.
- **Underline tabs** = `DetailTabs` with `variant="underline"` (default `pill` keeps prompts/skills markup byte-identical): no own `.card` wrapper; tablist `flex overflow-x-auto`, padding 0 32 (20 below 640), `border-bottom: 1px line2`; tab `button` padding 12/16, 14 px `muted`, `border-bottom: 2px transparent`, `-mb-px`; `aria-selected="true"` → `ink`, 600, `border-bottom-color: accent`; count = `Tab.count` rendered as `Label <span>n</span>` (one space, mono 11 `muted`, D71) on Stack and Articles liés only. Panels padding 28/32/32 (20 sides below 640). Roving tabindex, `data-js` fallback, D13 (< 2 tabs → no row) and fallback headings unchanged (`[data-detail-tabs]` stays on the root).
- **Tabs**: Aperçu when the body is not blank **or** `snippet` is set (cover no longer feeds it — it is the banner); Stack (n = stack length) when stack not empty; Articles liés (n = published related posts) when n > 0. Order Aperçu · Stack · Articles liés.
- **Aperçu**: the body as `.prose` (its first paragraph is the prototype's "why" paragraph, already 16 / 1.75), then, 24 px below, `CodeWindow` when `snippet` is set: `<figure data-code-window data-pagefind-ignore>` radius 14, `overflow-hidden`, border 1 px `windowLine`, **same colours in both themes**; header 38 px `windowHead`: three 10 px round dots (`windowDotRed/Amber/Green`, gap 6), filename chip = `snippetFile` (mono 11 `windowDim` on `windowLine`, radius 7; omitted without `snippetFile`), `Copier` at the right (mono 10 `windowDim`, hover `windowInk`, radius 7, rendered `hidden`, revealed and wired by `src/scripts/code-window.ts` → `copyText`, label `Copié !` 1.5 s, `aria-live="polite"`); body `windowBg`, `<pre>` `overflow-x-auto` mono 13 / 1.7, padding 16/16/16/0, grid `46px 1fr`: gutter `aria-hidden` `select-none` right-aligned numbers `windowDim` + `<code>` whose text is exactly the snippet minus one trailing newline; YAML highlighting when `snippetFile` ends `.yml`/`.yaml`: `key:` `windowKey`, the value after it `windowValue`, everything else (indent, `- `, block-scalar lines) `windowInk`. `copy-code.ts` skips any `pre` inside `[data-code-window]`.
- **Stack**: label `// ce qui fait tourner le projet` mono 12 `muted` (`data-pagefind-ignore`), then `<ul aria-label="Technologies">` grid `repeat(auto-fill, minmax(180px, 1fr))` gap 12; tile `.card-inner` radius 16, padding 16: 88 px logo slot (no own background) holding the Simple Icons path at 40 px `ink` (`aria-hidden`), or, without a logo, a monogram in mono 20 / 600 `muted` (`aria-hidden`: word initials up to 3 for multi-word names — `SC`, `GP` —, else the first 3 letters uppercased — `SVU`); name 15 / 600 `ink`; role mono 10 `muted`, omitted when unsourced.
- **Articles liés** panel: label `// écrit à propos de ce projet` (as Stack); one `.card-inner` `<a href="/blog/<id>/">` radius 16, padding 18 per post: category mono 10 `accent` · date (fr-FR long) mono 10 `muted`, title 17 / 500 `ink`, `Lire →` 13 / 600 `accent`.
- **Sidebar** `<aside aria-label="Le projet en bref" data-pagefind-ignore>`, gap 16: **meta card** `.card` radius 18 (`--radius-aside`), `box-shadow: var(--shadow)`, padding 20: `<dl>` rows label mono 11 `muted` / value right-aligned, separated by 1 px `line2` — `statut` (status `.pill` with `PROJECT_STATUS_META` chip classes), `depuis` (fr-FR month + year, 14 px `ink`, omitted without `startDate`), `licence` (14 px `ink`, omitted without `license`); then `code source ↗` full-width `<a>` `bg-accent text-accentInk` radius 12, 14 px / 600, padding 11 (omitted without `repoUrl`); then `démo ↗` full-width `bg-chip text-ink` radius 12 (only with `demoUrl` — kept from base, not in the prototype). **Articles liés card** (`.card` radius 18, padding 20, omitted when n = 0): label mono 10 uppercase .12em `muted`; one link per post: title 14 / 500 `ink` (hover `accent`), date mono 10 `muted`.
- **Tokens** (`@theme static` **and** `:root[data-theme="dark"]`, identical values — always dark, inventory §9; the audit enumerates names from the dark block): `--color-windowBg: #0B1017`, `--color-windowHead: #111823`, `--color-windowLine: rgba(255,255,255,.08)`, `--color-windowInk: #C6D2DE`, `--color-windowDim: #9AA6B4`, `--color-windowKey: #93C5FD`, `--color-windowValue: #7EE7B0`, `--color-windowDotRed: #FF5F57`, `--color-windowDotAmber: #FEBC2E`, `--color-windowDotGreen: #28C840`; radius `--radius-aside: 18px`. Contract colour count 37 → 47.
- **Schema** (optional, `src/content.config.ts` + `public/admin/config.yml`): `license` (string), `snippet` (string; CMS `text`), `snippetFile` (string), `stackRoles` (list of `{ name, role }`; CMS `list` with two `string` fields). No refine (the CMS test reads `schema.shape`).

Expected output of `node scripts/check-project.mjs` after `npm run build`:
```
projects: 2/2 · ← tous les projets → /projets/ · h1 · 1 main .card · 1 aside · underline tabs
gha-svu: Aperçu | Stack 4 | Articles liés 1 · banner derived · statut actif · depuis mai 2025 · licence — · code source ↗ https://github.com/bencatlab/gha-svu · démo — · aside articles 1
gha-svu stack: GitHub Actions (logo) · action composite | Bash (logo) | Go (logo) | SVU (SVU) · prochain numéro de version
gha-svu window: .github/workflows/check-pr.yml · 30 lines · yaml · 1 Copier · code = snippet
site-bencat: Aperçu | Stack 5 · banner derived · statut wip · depuis juillet 2026 · licence — · code source ↗ https://github.com/bendevcat/bendevcat.github.io · démo ↗ https://bendevcat.github.io/ · aside articles —
site-bencat stack: Astro (logo) · statique | Tailwind CSS (logo) · thème | TypeScript (logo) | Sveltia CMS (SC) · écrire sans toucher au code | GitHub Pages (GP) · déploiement
site-bencat window: .github/workflows/deploy.yml · 35 lines · yaml · 1 Copier · code = snippet
links: every internal href resolves in dist/
search: 2 fragments · title and prose indexed · sidebar, window and panel labels not indexed
```
Exit 1 if a page lacks one of these blocks; if the tab root lacks `data-tab-variant="underline"`; if a tab count differs from its panel's tile / link count; if a tile has neither `svg` nor monogram; if the gutter numbers ≠ 1…n or the `<code>` text + `\n` ≠ the entry's frontmatter `snippet` (parsed with the `yaml` devDependency); if the aside, the window or a panel label lacks `data-pagefind-ignore`; if an internal `href` (query stripped) does not resolve in `dist/`; or if a project fragment (`dist/pagefind/fragment/*.pf_fragment`, read as in `check-article.mjs`) lacks its title or first prose sentence, or contains `tous les projets`, `code source`, `Copier`, `ce qui fait tourner`, `écrit à propos` or `Le projet en bref`.

## Criteria
| ID | Criterion | Measure | Guarantee |
|---|---|---|---|
| R0 | From `/projets`, the user opens any project and sees the prototype's project page (inventory §4): a main surface card (banner, `← tous les projets`, title, description, **underline** tabs Aperçu · Stack (n) · Articles liés (n) — a tab without data is omitted) beside a 280 px sidebar (statut, depuis, licence, full-width `code source ↗`; Articles liés); Stack shows one tile per tech with its logo (text when none) and its role when sourced; Aperçu shows a dark line-numbered code window with Copier when the project has a code snippet | reachability walkthrough on `npm run build && npx astro preview`, light then dark (`localStorage.theme` + reload): `/` › Projets › each project (hero and grid card) › every block above present (licence row absent — no source) › click each tab, `← tous les projets`, each Articles liés tile and sidebar link → a 200 page that is not `/404`; `code source ↗` / `démo ↗` hrefs = `repoUrl` / `demoUrl`; R1–R21 hold | no |
| R1 | Tab rules | `npx vitest run src/lib/detailTabs.test.ts` → "keeps Aperçu when the body is blank but a snippet is set", "omits Aperçu when the body is blank and there is no snippet, even with a cover", "gives Stack and Articles liés their counts and Aperçu none" pass | yes |
| R2 | Code window lines | `npx vitest run src/lib/codeWindow.test.ts` → "numbers every line from 1, blank lines included, dropping one trailing newline", "colours a YAML key and the value after it", "leaves list dashes, indentation and block-scalar lines plain", "does not highlight a non-YAML file" pass | yes |
| R3 | Stack tiles | `npx vitest run src/lib/projectDetail.test.ts` → "gives a tile the logo of its tech, case-insensitively", "falls back to a monogram when no logo exists", "attaches a role only to a tech of the stack" pass | yes |
| R4 | Sidebar meta rows | `npx vitest run src/lib/projectDetail.test.ts` → "lists statut, depuis and licence in that order" and "omits depuis and licence when they are not set" pass | yes |
| R5 | Sourced content only | `npx vitest run src/lib/projectContent.test.ts` → "every stack role is a verbatim fragment of its project's own text" (fails on any role not found in title + description + body, `*` stripped, whitespace collapsed, or naming a tech outside `stack`) and "site-bencat's snippet is a contiguous run of .github/workflows/deploy.yml" pass; `node --input-type=module -e "import {parse} from 'yaml';import {readFileSync} from 'node:fs';process.stdout.write(parse(readFileSync('src/content/projects/gha-svu/index.md','utf8').split('---')[1]).snippet)" \| diff - <(git -C ~/workspace/tools/gha-svu show cbf8c36:.github/workflows/check-pr.yml)` → empty; `grep -c '^license:' src/content/projects/*/index.md` → `:0` on both lines | yes |
| R6 | Schema and CMS agree | `npx vitest run src/lib/cms-config.test.ts` → 0 failed; `sed -n '/^const projects/,/^});/p' src/content.config.ts \| grep -cE '^\s+(license\|snippet\|snippetFile\|stackRoles):'` → `4`; `sed -n '/^  - name: projects/,/^  - name: prompts/p' public/admin/config.yml \| grep -cwE 'name: (license\|snippet\|snippetFile\|stackRoles)'` → `4` | no |
| R7 | Built project pages | `npm run build && node scripts/check-project.mjs` → the 9 lines above, exit 0; exit 1 on each defect class listed under the block | no |
| R8 | Tab rows | `node scripts/check-detail-tabs.mjs` → 7 rows, exit 0: `projets/gha-svu: Aperçu \| Stack 4 \| Articles liés 1`, `projets/site-bencat: Aperçu \| Stack 5`, the 5 prompt / skill rows unchanged | no |
| R9 | Always-dark window tokens | `npx vitest run src/lib/contrast.test.ts src/lib/auditRendered.test.ts` → "reads the 47 colour tokens of both themes from global.css", "falls back on the 47 contract colour names of global.css" and "gives the window tokens the same value in both themes and keeps windowInk, windowDim, windowKey and windowValue at 4.5:1 on windowBg and windowHead" pass; `git diff 6c442c0 -- src/styles/global.css \| grep -cE '^\+\s*--color-window'` → `20`; `… \| grep -cE '^-\s*--color-'` → `0` | yes |
| R10 | Layout and header geometry | preview, 1280, both themes, 2 projects: sidebar width 280, main = 1180 − 306 (± 1), gap 26, main card top − header bottom = 40 ± 2; main card radius 20 bg `surface`; banner height 150, full card width; `← tous les projets` mono 11 `muted`; h1 32 px / 700; description 16 px `muted`, width ≤ 600; tab row padding-left 32, `border-bottom` 1 px `line2`; tabs padding 12/16, 14 px; selected tab `ink` / 600 / `border-bottom` 2 px `accent`, others `muted`; counts mono 11 `muted` | no |
| R11 | Code window | preview, 1280: the window's computed `background-color` (body `#0B1017`, header `#111823`) is identical in light and dark; 3 dots 10 px; chip text = `snippetFile`; gutter 46 px, first number `1`, last `30` (gha-svu) / `35` (site-bencat); a `key:` span computes `windowKey`, its value `windowValue`; exactly one `Copier` in the window, none added by `copy-code.ts`; clicking it shows `Copié !` and `navigator.clipboard.readText()` = the snippet without line numbers | no |
| R12 | Stack tiles | preview, 1280, both themes, Stack tab: tile count = stack length; tiles radius 16 bg `card`, logo slot 88 px high; Astro, Tailwind CSS, TypeScript, GitHub Actions, Bash, Go show an `svg`; Sveltia CMS, GitHub Pages, SVU show `SC`, `GP`, `SVU`; name 15 px / 600; role mono 10 `muted` exactly where Sources lists one | no |
| R13 | Articles liés and sidebar | preview, 1280, both themes: on gha-svu the Articles liés tile (radius 16, title 17 px / 500, `Lire →` 13 px / 600 `accent`) and the sidebar card link open `/blog/comment-jutilise-github-actions-au-quotidien/`; site-bencat has neither the tab nor the sidebar card; meta card radius 18, bg `surface`, `box-shadow` ≠ `none`; rows `statut` then `depuis`, no `licence`; `code source ↗` width = card content width (± 1), radius 12, bg `accent`, colour `accentInk`; `démo ↗` only on site-bencat | no |
| R14 | Keyboard and without JavaScript | on both pages: only the selected tab has `tabindex="0"`; ArrowRight / ArrowLeft / Home / End move focus and selection (one visible panel); in a `sandbox` iframe without scripts: tab row `display: none`, every panel and its `// label` fallback heading visible, no `Copier` visible, every link works | no |
| R15 | Below 1024 | preview, both themes, 375 and 768: one column, main card then sidebar, each as wide as the content column (± 1); 0 horizontal overflow (`scrollWidth` = `clientWidth`); the code window scrolls inside its `pre`; at 375 the three gha-svu tabs fit or scroll inside the tab row; at 1024 two columns | no |
| R16 | Search | `node scripts/check-project.mjs` → `search:` line as above; preview, search dialog: `SVU` → `gha-svu — le versionnage sémantique dans GitHub Actions` among the results; `tous les projets` → no `/projets/<id>/` result | no |
| R17 | Fidelity with the prototype | verifier renders the prototype (`claude-design` `render_preview`, project `bb013596-0cd6-4e70-afad-e92b42d3f7f6`, file `bencat_ Prototype cliquable.dc.html`, project screen; the URL is never written down) and `/projets/gha-svu/` at 1280, light and dark: a side-by-side table lists the same blocks in the same order and the same two columns, each tab's panel included; differences allowed only where the Design rules say so (no licence row, `démo ↗`, full body in Aperçu, `muted` for `dim`, monograms, real snippet). **If the MCP is unavailable: smoke**, static comparison with inventory §4 written in the evidence | no |
| R18 | Audit | `scripts/audit-rendered.js` on the preview, both projects, each tab selected in turn, light and dark, at 375, 768 and 1280: 0 overflow, 0 contrast failure, 0 off-token colour, 0 V2 jump | no |
| R19 | Radii and mono (V6 amended, V5) | preview, both themes, both projects: every non-zero computed `border-radius` ∈ {7, 8, 9, 10, 12, 13, 14, 16, 18, 20, 24, 999} px; back link, counts, labels, dates, status, roles, monograms, filename, `Copier`, gutter and code compute `"JetBrains Mono`; h1, description, prose, tile names, article titles compute `"Nebula Sans"` | no |
| R20 | Nothing else moves | after build: `check-shell-blog`, `check-article`, `check-lists`, `check-home`, `check-secondary` → their base lines, exit 0; `check-finition` → its base lines except `v2: 0 card-level on bg · 0 rail off card level · 11 rail column · 5 derived thumbnails`; `dist/prompts/*/index.html` and `dist/skills/*/index.html` identical to a base build's after replacing `/_astro/<name>.<hash>.<ext>` by `/_astro/<name>.<ext>`; pattern-class count (`sed -n '/@layer components {/,/^}/p' src/styles/global.css \| grep -cE '^\s+\.[a-z-]+ \{'`) → `4` | no |
| R21 | Frozen paths, suite, types | `git diff 6c442c0 --stat -- src/content/blog src/content/prompts src/content/skills docs/anti-drift .github/workflows package.json package-lock.json` → empty; `git diff 6c442c0 --name-only -- src/content` → the two `src/content/projects/*/index.md`; `npx vitest run` → 0 failed, > 312 tests; `npm run check` → `0 errors`; `grep -c 'export function matchesFilters' src/lib/projectFilters.ts` → `1` | no |

## Shared resources
- `src/styles/global.css` — 10 window colour tokens (both blocks) + `--radius-aside`; plans 16–18 add the other always-dark windows next to them. No fifth pattern class.
- `src/components/DetailTabs.astro`, `src/lib/detailTabs.ts`, `src/scripts/detail-tabs.ts` — also prompts and skills (pill variant must stay byte-identical, R20).
- `src/components/Thumbnail.astro` — also home and lists; a new size only.
- `src/scripts/copy-code.ts` — articles, prompts, skills; one exclusion only.
- `scripts/audit-rendered.js` (+ its test), `src/lib/contrast.test.ts` — contract colour count 37 → 47.
- `src/content.config.ts`, `public/admin/config.yml` — plans 16–17 add fields too; additions only.
- `dist/`, port 4321. No version bump (D68).

## Tasks
### T1 — Detail data: tabs, code lines, stack tiles, meta rows
- Files: `src/lib/detailTabs.ts` + `detailTabs.test.ts` (`Tab.count?`; `ProjectTabInput.hasSnippet` replaces `hasCover`; the two cover tests become the R1 snippet tests), `src/lib/codeWindow.ts` + test (new), `src/lib/projectDetail.ts` + test (new: `stackTiles`, `monogram`, `projectMetaRows`; no `astro:content` import)
- Covers: R1, R2, R3, R4
- Acceptance: `npx vitest run src/lib/detailTabs.test.ts src/lib/codeWindow.test.ts src/lib/projectDetail.test.ts` → the 12 named tests pass, 0 failed; `npm run check` → `0 errors`
- Depends on: —

### T2 — Schema, CMS and sourced project content
- Files: `src/content.config.ts`, `public/admin/config.yml`, `src/content/projects/gha-svu/index.md`, `src/content/projects/site-bencat/index.md` (frontmatter only: `snippet`, `snippetFile`, `stackRoles` per Sources; no `license`), `src/lib/projectContent.test.ts` (new)
- Covers: R5, R6
- Acceptance: `npx vitest run src/lib/cms-config.test.ts src/lib/projectContent.test.ts` → 0 failed; the R5 `diff` command → empty; the R6 greps → as stated; `npm run build` → exit 0
- Depends on: —

### T3 — Always-dark window tokens
- Files: `src/styles/global.css` (10 colour tokens in both blocks, `--radius-aside`), `scripts/audit-rendered.js` (`CONTRACT_COLOR_NAMES` + comments 37 → 47), `src/lib/auditRendered.test.ts`, `src/lib/contrast.test.ts`
- Covers: R9
- Acceptance: `npx vitest run src/lib/contrast.test.ts src/lib/auditRendered.test.ts src/lib/palette.test.ts` → the 3 R9 tests pass, 0 failed; the two R9 `git diff` counts → `20` and `0`; `npm run build` → exit 0
- Depends on: —

### T4 — Underline variant of the tabs
- Files: `src/components/DetailTabs.astro` (`variant` prop, default `pill`; `data-tab-variant` only on the underline root; count span)
- Covers: R8 (component), R20 (pill unchanged)
- Acceptance: `npm run build && node scripts/check-detail-tabs.mjs` → the 7 base rows, exit 0; `dist/prompts/*/index.html` and `dist/skills/*/index.html` identical to the base build's after hash normalisation (R20 command, scratch base build in the scratchpad)
- Depends on: T1

### T5 — The project page
- Files: `src/pages/projets/[...slug].astro`, `src/components/project/{ProjectHeader,CodeWindow,StackTiles,RelatedPostTiles,ProjectAside}.astro` (new), `src/components/Thumbnail.astro` (`banner` size), `src/scripts/code-window.ts` (new), `src/scripts/copy-code.ts` (skip `[data-code-window] pre`), `scripts/check-project.mjs` (new, like `check-article.mjs`; imports `yaml` only)
- Covers: R7, R8, R16 (built), R20; the rendered rows R10–R15, R17–R19 are measured by the verifier
- Acceptance: `npm run build && node scripts/check-project.mjs` → the 9 expected lines, exit 0; `node scripts/check-detail-tabs.mjs` → the R8 rows; the six other instruments → the R20 lines; `npx vitest run` → 0 failed; `npm run check` → `0 errors`; `grep -rn 'code-window\|data-code-window' src/scripts/copy-code.ts` → 1 match or more
- Depends on: T1, T2, T3, T4

## Out of scope
- The prompt and skill pages' own layouts and their pill tab track (plans 16–17); a licence value (no source); roles for TypeScript, Bash, Go; covers or demo images; project tags on the detail page; a sticky sidebar; a "prochain projet" link; selection kept in the URL (D14).
- Any change to posts, prompts, skills content, `.github/workflows/**`, `docs/anti-drift/**`; version bump, tag, merge, push, deploy.
